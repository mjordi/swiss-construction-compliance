# Daily Product Improvement Decision — 2026-06-22

## 7-day merged PR calibration

Recent first-parent merges on `origin/main`:

- #138 `pipeline/2026-06-21-defect-input-label` — dashboard defect input label accessibility
- #137 `pipeline/2026-06-20-login-duplicate-submit-guard` — duplicate auth submit guard
- #136 `pipeline/2026-06-19-invalid-case-url-cleanup` — URL cleanup for invalid dashboard case handoff
- #135 `pipeline/2026-06-18-supabase-client-stability` — dashboard Supabase client identity stability
- #134 `pipeline/2026-06-17-vault-nav` — dashboard vault navigation
- #133 `pipeline/2026-06-16-cases-supabase-memo` — cases Supabase client stability
- #132 `pipeline/2026-06-15-vault-pending-navigation` — pending vault handoff lock

Calibration: recent throughput supports one small, focused, test-backed UI reliability/accessibility slice. No open PRs were found, so there is no active-branch overlap.

## Scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Label the dashboard signature capture surface | 7 | 9 | 7 | 2 | 9 | APPROVED |
| M — Stabilize dashboard handover recovery after linked-case refresh failures | 7 | 6 | 8 | 5 | 6 | BACKLOG |
| L — Guided protocol finalization review workspace | 9 | 3 | 9 | 8 | 4 | BACKLOG |

## Rationale

- **Approved:** The signature capture accessibility fix is adjacent to yesterday’s defect input label work, but covers a different Step 2 control. It is small enough for one reviewable PR and has clear user value for assistive technology users.
- **Backlogged:** The linked-case recovery coverage is valuable but larger than today’s safest reviewable slice because it touches several async states.
- **Backlogged:** The guided finalization workspace is strategically strong but too large for the calibrated daily slice; it should be broken into a future phase-1 plan.

## Engineering task

Implement Proposal S only:

1. Add a programmatic accessible name to the Step 2 signature capture canvas/region using the existing `label-signature` translation.
2. Add a focused regression test proving the signature capture surface is discoverable by that label after completing Step 1.
3. Run required validation: `npm run test`, `npm run lint`, `npm run build`.

## Result

Implemented on branch `pipeline/2026-06-22-signature-label`:

- Replaced the standalone Step 2 signature text label with a stable labelled signature capture group.
- Added `aria-labelledby="dashboard-signature-label"` and `role="group"` to the signature capture surface while preserving the pending `aria-disabled` state.
- Added a focused regression test in `__tests__/dashboard-photo-vault-handoff.test.tsx` proving the signature capture area is discoverable by the visible `label-signature` label.

Validation passed:

- `npm run test` — 30 files / 207 tests passed
- `npm run lint` — passed with no warnings after the role adjustment
- `npm run build` — passed

PR: pending creation.
