# Daily Product Improvement Decision — 2026-07-11

## 7-day merged-PR calibration

Recent first-parent merged PRs in the last 7 days are focused, test-backed S-to-bounded-M slices: reminder export summaries, vault readiness explainability, dashboard handoff feedback, protocol final review, idempotent case reminder exports, and dashboard deadline `.ics` feedback. This supports one small frontend product-confidence improvement with focused tests, lint, and build. Open PR #157 (`pipeline/2026-07-10-linked-case-checklist-sync`) touches dashboard protocol linked-case checklist sync, so today should avoid `app/dashboard/page.tsx` and choose a complementary cases-row export-confidence slice.

## 14-day strategic portfolio check

- **Mix:** Recent work is predominantly user-facing workflow reliability/legal-confidence improvements, not purely internal fixes. It spans public calculators, cases, vault, dashboard protocol handoffs, reminders, and artifact exports.
- **Size:** Mostly S-sized with several bounded Medium/phase-1 strategic bridges. This is appropriate for daily autonomous delivery, but the portfolio should keep pushing toward cross-workflow audit readiness.
- **Repeated surfaces:** Cases and deadline reminders have been touched often, but today's case export feedback is complementary to the recently merged idempotency fix and avoids the open dashboard PR.
- **Backlogged strategic themes:** Unified activity timeline / compliance cockpit remains repeatedly backlogged. It is still too large for one run while #157 is open and while underlying artifact confidence states are still being hardened.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Confirm case deadline reminder exports inline | 7 | 10 | 8 | 2 | 10 | APPROVED |
| M — Add a case row artifact-action summary | 8 | 7 | 9 | 5 | 8 | BACKLOG |
| L — Build an audit-ready legal artifact activity timeline | 10 | 3 | 10 | 8 | 5 | BACKLOG |

## Approved

### APPROVED — Confirm case deadline reminder exports inline

**User-impact note:** Target users are Swiss construction owners, contractors, and project admins exporting a 60-day notice reminder from a tracked case while relying on the follow-up checklist as an audit/readiness ledger. The workflow moment is clicking the case-level `.ics` export in the follow-up details. This is not just incremental polish: the export is a legal deadline artifact, and users should receive localized confirmation or failure feedback rather than a silent browser side effect.

**Engineering task:** In `app/dashboard/cases/page.tsx`, add per-case temporary success/error feedback around `downloadCaseReminder`, clear stale feedback when the same case starts a new export or the component unmounts, and preserve the existing explicit `calendarReminderExported: true` persistence. Add focused coverage in `__tests__/cases-checklist-persistence.test.tsx` for success feedback and export failure feedback.

## Backlogged

### BACKLOG — Add a case row artifact-action summary

Valuable Medium follow-up toward a command-center experience, but broader than today's feedback slice because it needs copy and layout for multiple artifact types, not just the case reminder export action.

### BACKLOG — Build an audit-ready legal artifact activity timeline

Strategically strongest L-sized direction, but too broad for one autonomous PR and likely to overlap emerging linked-case/dashboard work. A safer phase-1 would first define a shared activity derivation helper from existing protocol, case, checklist, vault, and reminder data.

## Rejected

None.

## Product review after implementation

The shipped slice still matches the approved user-impact note: the case-level `.ics` reminder export now gives temporary localized success/error feedback beside the export action, while preserving the existing idempotent `calendarReminderExported: true` checklist persistence. The change is visible in a legal deadline artifact workflow and does not alter deadline math, URL filters, protocol/vault handoffs, or persistence schema.

## Validation

- `npm run test -- __tests__/cases-checklist-persistence.test.tsx` — passed (8 tests)
- `npm run test` — passed (31 files, 232 tests)
- `npm run lint` — passed
- `npm run build` — passed

## PR link

Not opened yet.
