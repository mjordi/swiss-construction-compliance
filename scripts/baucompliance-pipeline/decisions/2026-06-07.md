# Daily Product Improvement Decision — 2026-06-07

## 7-day calibration

Recent first-parent merges on `origin/main`:

- #123 `pipeline/2026-06-06-ruegefrist-reminder-presets` — reminder preset controls and tests for Rügefrist/deadline calculators.
- #122 `pipeline/2026-06-05-ruegefrist-share-links` — share links and follow-up fixes for Rügefrist query-param hydration.
- #121 `pipeline/2026-06-04-dashboard-case-handoff-consume` — dashboard case handoff hydration reliability.
- #120 `pipeline/2026-06-03-case-checklist-row-lock` — row-level pending locks for checklist actions.
- #119 `pipeline/2026-06-02-protocol-finalize-pending-guard` — finalization pending guard.
- #118 `pipeline/2026-06-01-case-create-pending-guard` — case-create pending guard.
- #117 `pipeline/2026-05-31-settings-profile-save-race` — profile-save race protection.

The throughput supports a small, focused follow-up or a bounded medium slice. No open PRs were found with `gh pr list`, so overlap risk is low. Because yesterday's merge touched reminder presets directly, today's safest choice is a complementary accessibility completion rather than a broader refactor.

## PM scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Mark reminder preset toggles with pressed state | 7 | 9 | 7 | 2 | 9 | APPROVED |
| M — Unify reminder preset UI into a shared component | 6 | 7 | 7 | 5 | 6 | BACKLOG |
| L — Add a consolidated deadline command center | 9 | 3 | 9 | 8 | 4 | BACKLOG |

## Final decisions

### APPROVED: S — Mark reminder preset toggles with pressed state

Rationale: This completes the accessibility contract for a newly shipped reminder feature, has clear user value, and can be validated with focused component tests plus the normal test/lint/build gates. It is small enough for one reviewable PR and should not conflict with current work.

**Engineering task:** Add `aria-pressed={reminderOffsets.includes(offset)}` to reminder preset buttons in `components/ruegefrist-calculator.tsx` and `app/dashboard/deadlines/page.tsx`. Add/extend tests in `__tests__/ruegefrist-calculator.test.tsx` and `__tests__/deadlines-share-link.test.tsx` to assert default selected presets, toggled state changes, and URL-hydrated custom reminder state.

### BACKLOG: M — Unify reminder preset UI into a shared component

Rationale: Valuable, but a refactor across both calculator surfaces is larger than necessary immediately after yesterday's reminder preset merge. Backlog until there is another reminder UI change or duplication causes review churn.

### BACKLOG: L — Add a consolidated deadline command center

Rationale: Strategically strong but clearly beyond recent daily-shippable scope. It needs discovery/design and likely multiple PRs spanning cases, vault, dashboard deadline aggregation, and exports.

### REJECTED

None.

## Engineering outcome

Implemented in branch `pipeline/2026-06-07-reminder-aria-pressed`.

Validation passed:

- `npm run test` — 26 files / 182 tests passed.
- `npm run lint` — passed.
- `npm run build` — passed.

PR: https://github.com/mjordi/swiss-construction-compliance/pull/124
