# Daily Product Improvement Decision — 2026-06-12

## 7-day calibration

Recent first-parent merges:

- #128 `pipeline/2026-06-11-dashboard-label-accessibility` — small dashboard label/accessibility fix plus focused tests.
- #127 `pipeline/2026-06-10-settings-labels` — small settings label/accessibility fix plus tests.
- #126 `pipeline/2026-06-09-password-save-race` — focused settings async race fix.
- #125 `pipeline/2026-06-08-clear-stale-deadlines` — focused deadlines stale-results fix.
- #124 `pipeline/2026-06-07-reminder-aria-pressed` — small accessibility sweep.
- #123 `pipeline/2026-06-06-ruegefrist-reminder-presets` — bounded reminder-preset feature.
- #122 `pipeline/2026-06-05-ruegefrist-share-links` — focused share-link/date state improvement.

This supports approving one focused reliability/UX change today. The Medium dashboard download-feedback slice is larger than the smallest recent label fixes but comparable to the recent stale async feedback and share-link guard work because it is limited to one component, one async action, localized copy, and focused tests. There are no open PRs, so overlap risk is low.

## PM scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
|---|---:|---:|---:|---:|---:|---|
| Disable the success-step PDF download button while generation is pending | 5 | 10 | 6 | 2 | 10 | BACKLOG |
| Add localized dashboard PDF download feedback with stale-completion guards | 8 | 8 | 8 | 4 | 8 | APPROVED |
| Case detail workspace for protocol, checklist, vault, and deadline state | 9 | 3 | 9 | 8 | 4 | BACKLOG |

## Rationale

- **APPROVED — Add localized dashboard PDF download feedback with stale-completion guards:** This was already identified as a useful next slice, improves operator trust at the end of the core handover workflow, and matches the repo’s recent successful pattern of small async-state hardening with focused tests.
- **BACKLOG — Disable the success-step PDF download button while generation is pending:** Useful but too narrow by itself; it is better delivered as part of the approved feedback/guard work.
- **BACKLOG — Case detail workspace for protocol, checklist, vault, and deadline state:** Strategically strong but too broad for today’s daily cadence without a separate phased design.

## Engineering task

On branch `pipeline/2026-06-12-dashboard-download-feedback`:

1. Add focused failing tests in `__tests__/dashboard-linked-case-retry.test.tsx` for dashboard PDF download success feedback, failure feedback, and ignoring a stale in-flight completion after starting a new protocol.
2. Update `app/dashboard/page.tsx` so `handleDownload` tracks a request id, clears stale feedback on new protocol, disables the download button while pending, shows localized success/failure feedback, and ignores outdated completions.
3. Add translation keys for the new feedback strings in `locales/index.ts` for de/fr/it/en.
4. Run focused dashboard tests, then required validation: `npm run test`, `npm run lint`, `npm run build`.

## PR

PR link: https://github.com/mjordi/swiss-construction-compliance/pull/129
