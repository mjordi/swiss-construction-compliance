# Daily Product Improvement Decision — 2026-07-30

## 7-day merged-PR calibration

After fetching and fast-forwarding to `origin/main`, first-parent history since 2026-07-23 shows seven focused product merges: Vault audit blockers (#170), legal milestone timeline (#171), linked finalized-protocol milestones (#172), case chronology CSV (#173), filtered case audit register (#174), protocol-finalization consequence guidance (#175), and an evidence-derived protocol PDF (#176). Recent throughput supports a bounded Medium, source-derived report slice with focused tests. `gh pr list --state open` returned no open PRs, so overlap risk is low.

## 14-day strategic portfolio check

- **User-facing vs internal:** The portfolio is predominantly visible legal-workflow confidence, readiness, chronology, and portable artifact work; internal reliability changes directly supported persisted/exported trust.
- **Size mix:** Early runs were mostly S-sized safeguards and scan signals; the later portfolio deliberately stepped up to bounded Medium timelines, exports, and evidence-derived records.
- **Repeated surfaces:** Cases and Vault are heavily optimized, and the last four runs already added chronology/export capabilities. A new isolated badge or wording patch would be marginal. Today's dossier is justified only as a consolidated external-review outcome rather than another internal card detail.
- **Strategic themes:** A durable evidence vault and append-only activity provenance remain the largest unsolved product promise. That direction needs storage, event integrity, RLS, retention, deletion, idempotency, privacy, and backfill decisions. A factual case-level dossier is a safe presentation/export prerequisite, not a substitute for those decisions.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | user-visible impact | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| S — Correct the Vault document-count claim | 7 | 10 | 7 | 1 | 10 | 7 | BACKLOG |
| M — Export a source-bound case audit dossier PDF | 9 | 8 | 9 | 4 | 9 | 10 | APPROVED |
| L — Build a real evidence vault with append-only activity provenance | 10 | 2 | 10 | 9 | 6 | 10 | BACKLOG |

## Approved

### APPROVED — Export a source-bound case audit dossier PDF

**User-impact note:** The target user is a Swiss owner, contractor, project administrator, compliance lead, or adviser preparing one defect case for notice, escalation, or external review. The workflow moment is the handoff from active in-app triage to a portable legal-review package. A real user will notice and benefit because one download will explain the source-derived legal status, deadline, next action, checklist gaps, and finalized protocol chronology without screenshots or manual spreadsheet assembly. This is not incremental polish; it creates a new case-level audit handoff.

**Explicit engineering task:** On branch `pipeline/2026-07-30-case-audit-dossier-pdf`, add a typed pure case-dossier report contract and an A4 `@react-pdf/renderer` component using the current `ComplianceCaseViewModel`, normalized checklist, and linked finalized protocol milestones. Add a per-case PDF action in expanded Cases details with deterministic filename, success/error feedback, stale async completion guards, and pending-action lockout. Add locale parity and focused contract, component, and UI coverage. Do not change Supabase persistence, deadline math, checklist semantics, protocol finalization, routes, database schema, evidence upload, or existing CSV/ICS exports.

## Backlogged

### BACKLOG — Correct the Vault document-count claim

This is a meaningful trust correction and should remain the fallback if the Medium dossier proves unsafe. It is not approved today because it would be another narrow wording patch after a portfolio of stronger bounded Medium outcomes.

### BACKLOG — Build a real evidence vault with append-only activity provenance

This is the highest strategic-value direction but unsafe for one autonomous PR. Its smallest credible phase requires an explicit private-storage/evidence metadata contract, event/RLS/retention/deletion semantics, and one idempotent source event while retaining source-derived chronology compatibility.

## Rejected

None.

## Engineering result

Implemented on `pipeline/2026-07-30-case-audit-dossier-pdf`:

- Added a typed, pure case-dossier contract that snapshots localized case identity, legal regime/status, key dates, localized next action, normalized checklist readiness/missing items, finalized linked-protocol count, and deterministic legal milestones.
- Treats the fixed calendar reminder as ready for old-law cases where no fixed notice deadline applies, matching the established audit-readiness contract instead of reporting a false blocker.
- Added an A4 source-bound PDF with factual case details, readiness, protocol source identifiers, chronology, generated-at timestamp, and the existing legal-advice disclaimer; it makes no compliance-certification claim.
- Added a localized per-case download action with deterministic filename, object-URL cleanup, success/error feedback, synchronous duplicate suppression, stale/unmounted completion guards, and same-case action/checklist/navigation lockout while generation is pending.
- Added DE/FR/IT/EN locale parity plus focused contract, PDF component, successful-download, and pending-lock regression coverage.

## Product review after implementation

The shipped slice matches the approved user-impact note. A user can now create one factual case-level review artifact from existing legal dates, readiness gaps, and finalized protocol chronology instead of assembling screenshots and separate exports. This is a new portable audit handoff, not internal cleanup. Supabase persistence, deadline calculations, checklist semantics, protocol finalization, routes, schema, evidence upload, and existing CSV/ICS behavior are unchanged.

## Validation

Passed:

- Red phase: `npm run test -- __tests__/case-audit-dossier.test.ts` failed because the report contract did not exist.
- Red phase: `npm run test -- __tests__/case-audit-dossier-pdf.test.tsx` failed because the PDF component did not exist.
- Focused: `npm run test -- __tests__/case-audit-dossier.test.ts __tests__/case-audit-dossier-pdf.test.tsx __tests__/cases-checklist-persistence.test.tsx` — 3 files / 25 tests passed.
- `npm run test` — 35 files / 282 tests passed.
- `npm run lint` — passed with no warnings after cleanup.
- `npm run build` — passed.
- `git diff --check` — passed.

## Review gates

- **Spec compliance: PASS** — the implementation is exactly the approved read-only source-derived PDF slice; it includes the required case/readiness/finalized-protocol chronology facts and pending-action safety, with excluded persistence/legal-math/schema/routing surfaces unchanged.
- **Code quality: APPROVED after review fixes** — corrected old-law non-applicable reminder semantics, separated legal chronology from finalized-protocol count, preserved raw protocol source IDs, added synchronous duplicate suppression and unmount/request guards, locked sibling same-case actions during generation, cleaned object URLs/timers, and added locale parity plus focused regressions.
- **Final integration:** A final fetch found `origin/main` unchanged at `6a59a14`, and GitHub still reported no open PRs, so there is no fresh overlap.

## PR link

Not opened yet.
