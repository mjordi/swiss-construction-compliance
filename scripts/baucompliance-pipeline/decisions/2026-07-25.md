# Daily Product Improvement Decision — 2026-07-25

## 7-day merged-PR calibration

`origin/main` first-parent history since 2026-07-18 shows six focused pipeline product merges plus security maintenance: linked-case checklist normalization (#165), evidence readiness (#166), notice readiness (#167), case audit readiness (#168), Vault blocker explanation (#170), and the first legal milestone timeline (#171). Recent throughput includes three bounded Medium read-only UI/domain slices and supports another focused Medium extension when it reuses established contracts. `gh pr list --state open` returned no open PRs, so current overlap risk is low.

## 14-day strategic portfolio check

The portfolio is predominantly user-facing workflow reliability and legal-readiness work rather than internal-only maintenance. Most choices were S-sized readiness/export/reliability slices; the last week deliberately stepped up to bounded Medium audit summaries and a first timeline. Cases and Vault are heavily optimized surfaces. The repeatedly backlogged theme is a unified audit/activity cockpit. Yesterday’s timeline delivered the prerequisite presentation contract and named timestamped linked-protocol events as the next credible phase, so approving that phase avoids falling back to marginal scan-card polish.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Show the latest finalized protocol date in the case action snapshot | 6 | 9 | 7 | 2 | 10 | REJECTED |
| M — Add finalized linked-protocol events to the legal milestone timeline | 9 | 8 | 10 | 4 | 9 | APPROVED |
| L — Build a persisted cross-workflow audit activity ledger | 10 | 3 | 10 | 8 | 6 | BACKLOG |

## Approved

### APPROVED — Add finalized linked-protocol events to the legal milestone timeline

**User-impact note:** The target user is a Swiss contractor, owner, or project administrator reviewing a defect dossier before notice, escalation, or audit preparation. The workflow moment is verifying the legal sequence and completed evidence actions in expanded Case details. This is not incremental polish: it adds the first actual cross-workflow evidence event to the audit chronology, so users no longer have to infer protocol finalization from a count or switch workflows.

**Engineering task:** On branch `pipeline/2026-07-25-linked-protocol-milestones`, extend the protocol query and fallback state in `app/dashboard/cases/page.tsx` to retain linked protocol id/status/created timestamp; extend `deriveCaseLegalMilestones` in `lib/case-timeline.ts` to accept finalized protocol events, discard invalid/non-finalized timestamps, and order multiple events deterministically with the existing legal dates; render localized finalized-protocol milestones in the existing ordered timeline; update test doubles and add focused domain/UI regression coverage. Do not change persistence, deadline math, protocol creation/finalization, filters, exports, or routes. Run `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Build a persisted cross-workflow audit activity ledger

Strategically strongest, but not safe in one daily PR because it requires schema/migration, authorization, privacy, event-integrity, and backfill decisions. The smallest credible next phase after today is to specify an append-only event model and persist one protocol-finalized event while continuing to display source-record-derived events as the compatibility path.

## Rejected

### REJECTED — Show the latest finalized protocol date in the case action snapshot

Safe and visible, but weaker than the approved chronology: it adds another isolated scan signal on an already heavily optimized surface and does not explain the event’s place in the legal sequence.

## Result

Implemented on branch `pipeline/2026-07-25-linked-protocol-milestones`:

- Cases now retain the linked protocol id, status, and creation timestamp already fetched alongside case records, including the established last-successful fallback path.
- The pure milestone helper adds only valid finalized-protocol source records, ignores drafts and invalid timestamps, and orders multiple protocol events deterministically with contract, discovery, and deadline milestones.
- Expanded Case details render localized finalized-protocol milestones in the existing semantic ordered timeline, with stable identities for repeated event kinds.
- Added focused domain and Cases UI regression coverage.

## Product review after implementation

The shipped slice matches the approved user-impact note. A contractor, owner, or project administrator can now verify when linked protocol evidence entered the dossier without leaving the Case chronology. This advances the audit/activity cockpit with a real cross-workflow evidence event rather than another isolated badge. The implementation is read-only and does not change persistence, deadline math, protocol creation/finalization, filters, exports, or routes.

## Validation

Passed:

- Red phase: `npm run test -- __tests__/case-timeline.test.ts -t "linked protocol"` failed before the domain helper accepted protocol events.
- Red phase: `npm run test -- __tests__/cases-checklist-persistence.test.tsx -t "protocol events"` failed before the Cases page retained and passed protocol event data.
- Focused: `npm run test -- __tests__/case-timeline.test.ts __tests__/cases-checklist-persistence.test.tsx` — 2 files / 28 tests passed.
- `npm run test` — 31 files / 257 tests passed.
- `npm run lint` — passed.
- `npm run build` — passed.

Controller review:

- Spec compliance: PASS — exactly the approved finalized linked-protocol timeline slice was implemented, with no persistence/routing/deadline scope expansion.
- Code quality: APPROVED — source rows use a narrow typed projection; invalid/non-finalized records are excluded; deterministic event identity/order and refresh fallback behavior are preserved; focused and full validation are green.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/172
