# Daily Product Improvement Decision — 2026-06-16

## 7-day calibration

First-parent merged PRs in the last 7 days:

- #132 — vault pending navigation guard
- #131 — login label accessibility
- #130 — case edit cache refresh
- #129 — dashboard PDF download feedback
- #128 — dashboard label accessibility
- #127 — settings labels
- #126 — password save race

Recent throughput supports one small, focused, test-backed reliability or accessibility patch per day. There are no currently open PRs, and yesterday's work touched the vault page, so today's safest low-overlap target is the adjacent cases page.

## Scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Stabilize the Cases page Supabase client | 7 | 9 | 8 | 2 | 9 | APPROVED |
| Normalize Supabase placeholder client identity globally | 7 | 7 | 8 | 4 | 7 | BACKLOG |
| Add a unified dashboard data-client provider | 8 | 4 | 8 | 7 | 4 | BACKLOG |

## Decisions

### APPROVED — Stabilize the Cases page Supabase client

This is a small reliability fix with clear user/developer value and little overlap with the recently merged vault navigation work. It follows an existing local pattern (`Vault` already memoizes `getSupabase`) and can be validated with one focused regression test plus the required repo validation.

**Engineering task:** In `app/dashboard/cases/page.tsx`, memoize the Supabase client using `useMemo(() => getSupabase(), [])`. Add a focused test in the cases test suite proving that rerendering the page does not call `getSupabase` again or retrigger the initial cases/protocols load when the mocked client factory would otherwise return a fresh object.

### BACKLOG — Normalize Supabase placeholder client identity globally

Valuable and likely the more systemic fix, but it touches shared client infrastructure and should be considered after the page-level behavior is stabilized and reviewed.

### BACKLOG — Add a unified dashboard data-client provider

Strategically useful, but too broad for the recent daily PR size and likely to create unnecessary review risk in one autonomous run.

## PR

PR link: Not opened in this cron run
