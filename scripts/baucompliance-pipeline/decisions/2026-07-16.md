# Daily Product Improvement Decision — 2026-07-16

## 7-day merged-PR calibration

Recent first-parent merged PRs in the last 7 days:

- #161 `pipeline/2026-07-14-linked-protocol-follow-up` — S dashboard linked protocol follow-up handoff.
- #160 `pipeline/2026-07-13-vault-audit-snapshot` — S Vault legal next-action/deadline snapshot.
- #159 `pipeline/2026-07-12-public-calculator-ics-feedback` — S public Ruegefrist `.ics` success/error feedback.
- #158 `pipeline/2026-07-11-case-reminder-export-feedback` — S case reminder export success/error feedback.
- #157 `pipeline/2026-07-10-linked-case-checklist-sync` — S linked protocol finalization checklist sync correctness.
- #156 `pipeline/2026-07-09-deadline-ics-feedback` — S dashboard deadline `.ics` export feedback.

Calibration: recent throughput supports one focused frontend product slice with a narrow regression test and full test/lint/build validation. The week is mostly S-sized, but the S work has been user-facing and legally meaningful. Today should not overlap the open #162 Vault archive-feedback PR; the safest useful slice is on the Cases scan surface.

Open PR overlap: #162 `pipeline/2026-07-15-vault-archive-feedback` is open and touches Vault archive/restore feedback, so Vault archive/status work is avoided today.

## 14-day strategic portfolio check

- **User-facing workflow improvements vs internal fixes:** Recent decisions are dominated by user-facing legal workflow reliability: reminder exports, protocol/case handoffs, Vault audit snapshots, linked-case checklist sync, and URL-state trust.
- **S vs M vs L/phase-1 strategic choices:** Mostly S-sized daily slices, with bounded Medium/phase-1 work around protocol final review and audit-context surfacing.
- **Repeated surfaces:** Cases, dashboard protocol, reminders, public calculators, and Vault have all been touched. Today's Cases badge is justified because it exposes an already-important reminder state at scan time rather than adding same-surface export polish.
- **Repeated strategic themes:** Unified audit/activity timeline remains repeatedly backlogged. Scan-level readiness signals are a prerequisite for that cockpit because they define which artifact states matter before timeline aggregation.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Surface calendar-reminder readiness on case cards | 8 | 10 | 8 | 2 | 10 | APPROVED |
| M — Add a read-only case activity snapshot row | 8 | 6 | 9 | 5 | 8 | BACKLOG |
| L — Build a unified audit-ready activity timeline | 10 | 3 | 10 | 8 | 5 | BACKLOG |

## Approved

### APPROVED — Surface calendar-reminder readiness on case cards

**User-impact note:** Target users are Swiss construction owners, contractors, and project admins scanning the Cases ledger. The workflow moment is triaging which defect cases still need deadline-protection actions before opening detail panels. This is not incremental polish: calendar reminders protect the 60-day notice workflow, and surfacing readiness on the collapsed case card reduces the chance that a legally important export remains hidden.

**Engineering task:** In `app/dashboard/cases/page.tsx`, add a compact localized reminder-readiness badge to the case-card header, using the existing `formatCaseReminderReadiness` helper and `reminderReadiness` state. Add focused coverage in `__tests__/cases-checklist-persistence.test.tsx` proving scan-level cases show pending reminder state without expanding details and update to ready after reminder export. Run `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Add a read-only case activity snapshot row

Valuable Medium follow-up toward the legal cockpit, but broader than today because it needs copy/layout decisions across protocol count, Vault evidence, next action, and reminder readiness.

### BACKLOG — Build a unified audit-ready activity timeline

Strategically strongest, but too broad for one daily PR. The smallest credible phase-1 is a derived read-only timeline helper for a single case surface after scan-level artifact states are stable.

## Rejected

None.

## Result

Implemented on branch `pipeline/2026-07-16-case-reminder-badge`:

- Case cards now show a localized scan-level reminder-readiness badge beside regime/status/checklist progress.
- The badge reuses the existing reminder readiness contract, so it shows calendar-ready, calendar-pending, or not-applicable state without changing deadline math, persistence, export behavior, URL state, or protocol/Vault handoffs.
- Added focused regression coverage for the scan-level pending reminder badge.

## Product review after implementation

The shipped slice matches the approved user-impact note: a user scanning the Cases ledger can see whether deadline-reminder export is still pending before opening details. The implementation is visible, narrow, and legally relevant because calendar reminders protect the notice workflow.

## Validation

Passed:

- `npm run test -- __tests__/cases-checklist-persistence.test.tsx` — passed (9 tests).
- `npm run test` — passed (31 files, 243 tests).
- `npm run lint` — passed.
- `npm run build` — passed.

## PR link

Not opened in this cron run
