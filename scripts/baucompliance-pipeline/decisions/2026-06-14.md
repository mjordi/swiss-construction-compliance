# Daily Product Improvement Decision — 2026-06-14

## 7-day calibration

Recent first-parent merged PRs in the last 7 days:

- `#129` / `886fe52` — dashboard PDF download feedback, 5 files, ~243 insertions / 8 deletions.
- `#128` / `7aa37f3` — dashboard wizard label accessibility, 4 files, ~90 insertions / 3 deletions.
- `#127` / `01bfc2f` — settings label accessibility, 4 files, ~92 insertions / 3 deletions.
- `#126` / `de851af` — password save race handling, 4 files, ~113 insertions / 7 deletions.
- `#125` / `4c9d0aa` — stale deadline/share state clearing, 5 files, ~127 insertions / 10 deletions.
- `#124` / `d3e144c` — reminder `aria-pressed` coverage, 5 files, ~131 insertions.

No open PRs were found with `gh pr list`, so there is no current branch overlap to avoid. This calibration supports one small-to-medium, tightly tested UI/accessibility improvement today.

## Scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Associate login form labels with their inputs | 7 | 9 | 8 | 2 | 10 | APPROVED |
| Add pending feedback to Vault archive/restore mutations | 7 | 7 | 7 | 4 | 8 | BACKLOG |
| Unified compliance action center | 9 | 3 | 9 | 8 | 4 | BACKLOG |

## Final decisions

### APPROVED — Associate login form labels with their inputs

Rationale: This is a high-confidence accessibility improvement on the product's entry point, directly aligned with the recent label/accessibility cadence while remaining smaller than the recent daily merged PRs. It is easy to validate with focused RTL tests plus required test/lint/build.

**Engineering task:** Add explicit `id`/`htmlFor` wiring for the login/signup name, email, and password fields in `app/login/page.tsx`; add a focused test file verifying fields can be queried by visible label in sign-in and sign-up modes and that toggling modes preserves accessible form controls.

### BACKLOG — Add pending feedback to Vault archive/restore mutations

Rationale: Valuable and likely shippable as a medium slice, but it touches async mutation state and needs careful i18n/test coverage. It is a good future candidate after today's lower-risk login accessibility fix.

### BACKLOG — Unified compliance action center

Rationale: Strong strategic direction, but too broad for one daily reviewable PR. It needs design and a phase-1 plan before implementation.

## Rejected

None.

## Engineering outcome

Implemented on branch `pipeline/2026-06-14-login-label-accessibility`:

- Added stable `id`/`htmlFor` associations for the login/signup full name, email, and password fields.
- Added `__tests__/login-label-accessibility.test.tsx` to verify sign-in and sign-up controls are queryable by visible labels while toggling modes.

Validation passed:

- `npm run test` — 27 files / 191 tests passed.
- `npm run lint` — passed.
- `npm run build` — passed.

PR opened: https://github.com/mjordi/swiss-construction-compliance/pull/131
