# Daily Product Improvement Decision — 2026-06-09

## 7-day calibration

Recent first-parent merges in the last 7 days include PR #119 through #124. They shipped focused, test-backed patches such as protocol pending guards, case checklist row locks, dashboard case handoff cleanup, Rügefrist share-link/reminder improvements, and reminder `aria-pressed` accessibility. Typical daily size is a narrow S/M slice with focused tests and validation. No open PRs were found via `gh pr list`, so overlap risk is low.

## PM scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S: Guard password update success against newer edits | 7 | 9 | 8 | 2 | 9 | APPROVED |
| M: Make password/profile settings controls fully labelled and pending-safe | 7 | 7 | 7 | 4 | 7 | BACKLOG |
| L: Account security activity and recovery surface | 8 | 4 | 8 | 7 | 4 | BACKLOG |

## Final decisions

### APPROVED — Guard password update success against newer edits

This is a small adjacent follow-up to the recently shipped settings profile save race fix. It is reviewable in one PR, has clear user impact, and can be validated with one focused regression test plus full `test`, `lint`, and `build`.

**Engineering task:** In `app/dashboard/settings/page.tsx`, capture the submitted password when `handleUpdatePassword` starts, keep a ref synchronized with the latest visible password, and only clear the field / show `settings-password-updated` if the visible value still matches the submitted value when the async update succeeds. Add a regression test to `__tests__/settings-password-feedback.test.tsx` using a controlled pending `updateUser` promise; edit the password field before resolving and assert the newer value remains visible, stale success feedback is not shown, and the update action remains available.

### BACKLOG — Make password/profile settings controls fully labelled and pending-safe

Valuable, but broader than today's daily slice because it touches multiple controls and interaction states. It can follow once the password race behavior is fixed.

### BACKLOG — Account security activity and recovery surface

Strategically useful for trust, but too large and dependency-sensitive for today's calibrated daily PR. It needs product and Supabase capability discovery first.
