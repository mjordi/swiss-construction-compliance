# Daily Product Improvement Decision — 2026-08-10

## 7-day merged-PR calibration

After fetching `origin/main`, first-parent history shows six merged product PRs in the last seven days: evidence upload activity (#181), evidence chronology (#182), guided password recovery (#183), notice source basis (#184), notice preview (#185), and immutable notice revisions (#186). The latest merges range from 365 to 1,179 added lines and are bounded Medium/strategic-phase slices with focused tests and full test/lint/build validation. This throughput supports one coherent cross-surface Medium, but the three consecutive notice PRs make another notice implementation a poor overlap choice. GitHub reports no open PRs.

## 14-day strategic portfolio check

- **User-facing vs internal:** The 13 available daily decisions are overwhelmingly user-facing legal-confidence improvements; technical/security work directly supported those outcomes.
- **Size mix:** All 13 approvals were Medium or bounded strategic phases; no Small proposal was approved, so recent throughput is not imposing a polish ceiling.
- **Repeated surfaces:** Cases/Vault/evidence dominate. The last three runs deliberately advanced source-bound notice preparation; another notice slice today would over-concentrate the portfolio.
- **Repeated strategic themes:** Active/auditable reminders remain a strategic gap, while false email-reminder readiness was explicitly backlogged on 2026-08-09. A truthful cross-product activation contract is the safest prerequisite before server scheduling can be designed.

## Scoring and decisions

Scores are 1–10, where higher risk means more risk.

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | user-visible impact | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| S — Correct false Case email-reminder readiness | 8 | 10 | 7 | 2 | 10 | 8 | REJECTED |
| M — Make reminder delivery truth and activation explicit across the product | 9 | 9 | 9 | 3 | 10 | 9 | APPROVED |
| L — Auditable active deadline notification engine | 10 | 2 | 10 | 9 | 7 | 10 | BACKLOG |

## Approved

### APPROVED — Make reminder delivery truth and activation explicit across the product

**User-impact note:** The target user is a Swiss owner, contractor, project administrator, or adviser immediately after calculating or reviewing a legally important deadline. A real user will notice and benefit because the product will stop claiming that an email reminder is planned and will explain the final action needed to activate selected calendar reminders. This is not cosmetic copy: it removes a false legal-safety signal and establishes one truthful delivery contract across Case and calculator workflows, while shifting today’s work away from the already concentrated notice surface.

**Explicit engineering task:** On branch `pipeline/2026-08-10-reminder-delivery-truth`, first add failing focused tests proving that Case timeline models never claim an email reminder, that Case and both calculator export surfaces explain `.ics` import activation/no email or in-app delivery, and that successful downloads prompt import. Then minimally update `lib/case-timeline.ts`, `app/dashboard/cases/page.tsx`, `app/dashboard/deadlines/page.tsx`, `components/ruegefrist-calculator.tsx`, and DE/FR/IT/EN keys in `locales/index.ts`. Reuse the existing feedback/request guards. Do not change deadline math, selected offsets, ICS generation, persistence, scheduling, notification infrastructure, Case notice behavior, or database schema. Run focused tests, then required `npm run test`, `npm run lint`, and `npm run build`; do not complete or commit the run if any required validation remains failing.

## Backlogged

### BACKLOG — Auditable active deadline notification engine

Strategically strongest end state, but unsafe for one daily PR. Tenant isolation, channel consent/preferences, secure server scheduling, idempotency, retry/failure semantics, immutable delivery evidence, privacy, and retention require explicit design. The smallest credible future phase is the tenant-isolated subscription and delivery-attempt contract after those boundaries are reviewed.

## Rejected

### REJECTED — Correct false Case email-reminder readiness

The correction is valuable, but the approved Medium subsumes it and also fixes the adjacent activation ambiguity on every existing reminder-export surface. Shipping only the Small slice would leave users unclear about how calculator reminders become active.

## Engineering result

Implemented on `pipeline/2026-08-10-reminder-delivery-truth`:

- Corrected both old- and new-law Case timeline view models so they no longer claim an email reminder is planned.
- Added one localized DE/FR/IT/EN reminder-delivery contract across Cases, the authenticated deadline calculator, and the public Rügefrist calculator: selected offsets are embedded in `.ics`, calendar import is required, and BauCompliance sends no email or in-app reminders.
- Added semantically named reminder activation/export regions so guidance is associated with the controls that consume it, including the expanded Case export surface.
- Added accurate `reminders=none` behavior: calculator exports are described as deadline events without alerts/reminders, both before export and in success feedback.
- Updated public deadline marketing copy in all four locales to promise automatic calculation plus an explicit downloadable/import-required calendar handoff rather than active monitoring or delivered alerts.
- Preserved existing deadline calculations, reminder offsets, ICS generation, async request/version guards, feedback timeouts, persistence, database schema, and notice workflows.
- Added focused domain, UI placement, empty-reminder, feedback, marketing-truth, Italian voice, and locale-parity regressions.

## Product review after implementation

The shipped slice matches the approved user-impact note. A Swiss construction user can now distinguish a downloaded calendar artifact from an active BauCompliance notification service at every existing reminder workflow. Users see the exact import action needed to activate selected reminders, while a no-reminders export is honestly described as event-only. This is a meaningful legal-trust correction, not cosmetic copy, and no active-delivery capability is claimed or simulated.

## Validation

Passed:

- Red phase: 7 focused assertions failed before the reminder truth/activation implementation existed; 103 prior assertions remained green.
- Focused final suite: 5 files / 113 tests passed.
- Required `npm run test` — 45 files / 426 tests passed.
- Required `npm run lint` — passed with no errors.
- Required `npm run build` — passed; Next.js compiled, TypeScript completed, and all 15 pages/routes were generated or classified successfully.
- `git diff --check` — passed.
- The suite retains pre-existing non-failing JSDOM navigation/canvas messages and calendar-day React `act(...)` warnings.

Review gates:

- Spec compliance: PASS.
- Code quality/legal trust: APPROVED after correcting public marketing claims, adding semantic placement tests, standardizing Italian voice, and handling `reminders=none` without false activation claims.
- Final overlap check: `origin/main` remains at `1b5b552` and GitHub reports no open PRs.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/187
