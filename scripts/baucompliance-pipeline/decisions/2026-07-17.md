# Daily Product Improvement Decision — 2026-07-17

## 7-day merged-PR calibration

Recent first-parent merged PRs in the last 7 days:

- #162 `pipeline/2026-07-15-vault-archive-feedback` — S Vault archive/restore confirmation.
- #163 `pipeline/2026-07-16-case-reminder-badge` — S scan-level case reminder readiness badge.
- #161 `pipeline/2026-07-14-linked-protocol-follow-up` — S dashboard linked protocol follow-up handoff.
- #160 `pipeline/2026-07-13-vault-audit-snapshot` — S Vault legal next-action/deadline snapshot.
- #159 `pipeline/2026-07-12-public-calculator-ics-feedback` — S public Ruegefrist `.ics` success/error feedback.
- #158 `pipeline/2026-07-11-case-reminder-export-feedback` — S case reminder export success/error feedback.
- #157 `pipeline/2026-07-10-linked-case-checklist-sync` — S linked protocol finalization checklist sync correctness.

Calibration: recent throughput strongly supports one focused frontend product slice with localized copy, focused regression coverage, and full `npm run test`, `npm run lint`, and `npm run build` validation. The last week is mostly S-sized, but consistently user-facing and legally meaningful. Today should continue stepping toward the recurring audit-cockpit theme without taking on the full timeline model.

Open PR overlap: `gh pr list --state open` returned no open PRs, so there is no known active branch overlap.

## 14-day strategic portfolio check

- **User-facing workflow improvements vs internal fixes:** Recent decisions are dominated by user-facing legal workflow confidence: reminder export feedback/idempotency, case/protocol handoffs, Vault audit snapshots, archive/restore confirmation, scan-level readiness, and URL-state reliability.
- **S vs M vs L/phase-1 strategic choices:** Mostly S-sized daily slices, with bounded phase-1 movement around protocol final review, Vault audit context, and case scan-level readiness.
- **Repeated surfaces:** Cases, dashboard protocol, Vault, reminders, public calculators, and deadlines have all been touched. A small Cases follow-up is justified because it fills a missing artifact dimension in the new action snapshot rather than repeating the exact reminder badge work.
- **Repeated strategic themes:** Unified audit/activity timeline remains repeatedly backlogged. The next prerequisite is making case-level artifact signals complete and stable before deriving a cross-workflow chronology.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Show linked protocol count in the case action snapshot | 7 | 10 | 8 | 2 | 10 | APPROVED |
| M — Add a first read-only case activity summary row | 8 | 6 | 9 | 5 | 8 | BACKLOG |
| L — Build a unified audit-ready activity timeline | 10 | 3 | 10 | 8 | 5 | BACKLOG |

## Approved

### APPROVED — Show linked protocol count in the case action snapshot

**User-impact note:** Target users are Swiss construction owners, contractors, and project admins scanning the Cases ledger. The workflow moment is deciding whether a defect case already has protocol evidence supporting the next legal action. This is not just incremental polish: protocol records are part of the legal evidence trail, and a best-in-market compliance product should show that artifact state beside deadline and reminder readiness.

**Engineering task:** In `app/dashboard/cases/page.tsx`, add a localized linked-protocols cell to the existing scan-level `cases-action-snapshot-*` row, using the already-loaded `protocolCounts`. Add localized keys in `locales/index.ts` and focused coverage in `__tests__/cases-checklist-persistence.test.tsx` proving a case with linked protocols shows the count in the scan-level action snapshot. Run `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Add a first read-only case activity summary row

Valuable Medium follow-up toward the legal cockpit, but broader than today because it should include a concise Vault evidence/status contract in addition to protocol, reminder, checklist, and next-action context.

### BACKLOG — Build a unified audit-ready activity timeline

Strategically strongest, but too broad for one daily PR. The smallest credible future phase-1 is a derived read-only timeline helper for one case surface once scan-level artifact signals are complete and stable.

## Rejected

None.

## Result

Implemented on branch `pipeline/2026-07-17-case-protocol-snapshot`:

- Case action snapshots now show a localized linked-protocols cell beside next action, deadline countdown, and reminder readiness.
- The snapshot reuses the existing `protocolCounts` query, so it adds scan-level audit context without changing persistence, protocol creation, deadline math, reminders, Vault links, or URL state.
- Added focused regression coverage proving a case with linked protocols shows the count in the scan-level action snapshot.

## Product review after implementation

The shipped slice matches the approved user-impact note: a user scanning the Cases ledger can see whether protocol evidence already exists before opening details or creating another protocol. The implementation is visible, narrow, localized, and advances the audit-cockpit theme by filling one missing artifact dimension in the case scan surface.

## Validation

Passed:

- `npm run test -- __tests__/cases-checklist-persistence.test.tsx` — passed (10 tests).
- `npm run test -- __tests__/cases-checklist-persistence.test.tsx __tests__/cases-url-sync.test.tsx __tests__/deadlines-share-link.test.tsx` — passed (48 tests) after isolating the initial full-suite failures.
- `npm run test` — passed (31 files, 248 tests) on rerun. Initial run had one product-code-adjacent duplicate text failure in `cases-url-sync`, fixed by rendering the protocol count as a numeric value under the localized label, plus a known transient `deadlines-share-link` reset flake that passed on focused and full rerun.
- `npm run lint` — passed.
- `npm run build` — passed.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/164
