# Daily Product Improvement Decision — 2026-05-30

## 7-day calibration

First-parent merged work in the last 7 days includes PRs #109, #110, #111, #112, #113, #114, and #115, plus a small refresh-failure fix on main. The recent cadence supports small-to-medium UI reliability and workflow handoff slices with focused tests, but not broad multi-surface product funnel work in one unattended day. There are no open PRs at approval time, so overlap risk is low.

## PM scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
|---|---:|---:|---:|---:|---:|---|
| S — Clear stale Rügefrist results when calculator inputs change | 7 | 9 | 8 | 2 | 9 | APPROVED |
| M — Add a Rügefrist share link for calculated dates | 8 | 6 | 8 | 5 | 7 | BACKLOG |
| L — Guided public-to-dashboard defect workflow | 9 | 3 | 9 | 8 | 4 | BACKLOG |

## Rationale

- **Approved:** The Small proposal removes a concrete stale-result hazard in a legal-deadline calculator and improves control accessibility. It is narrow, testable, and calibrated to the repo's daily reviewable-change size.
- **Backlogged:** The Medium share-link proposal is valuable and phaseable, but it adds URL state/copy-feedback complexity that should follow after the calculator's current input/result contract is made safer.
- **Backlogged:** The Large guided handoff is strategically strong, but too broad for today's daily ship window and should start with product design plus an explicit phase-1 plan.
- **Rejected:** None.

## Engineering task

Implement the approved Small proposal:

1. In `components/ruegefrist-calculator.tsx`, give the date inputs and icon-only actions explicit accessible names.
2. Clear the currently displayed `RuegefristResult` whenever either source date input changes after a calculation, so results and exports cannot stay stale against edited inputs.
3. Add a focused component regression test for the stale-result clearing behavior.
4. Run required validation: `npm run test`, `npm run lint`, and `npm run build`.

## Validation

- `npm run test -- __tests__/ruegefrist-calculator.test.tsx` — passed (1 test).
- `npm run test -- __tests__/legal-utils.test.ts __tests__/ruegefrist-calculator.test.tsx` — passed (2 files, 24 tests).
- `npm run test` — passed (25 files, 163 tests).
- `npm run lint` — passed.
- `npm run build` — passed.

## PR

PR link: https://github.com/mjordi/swiss-construction-compliance/pull/116
