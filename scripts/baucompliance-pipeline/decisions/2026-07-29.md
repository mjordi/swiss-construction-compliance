# Daily Product Improvement Decision — 2026-07-29

## 7-day merged-PR calibration

After fetching and fast-forwarding to `origin/main`, first-parent history since 2026-07-22 shows six focused product merges: Vault audit blockers (#170), legal milestone timeline (#171), linked finalized-protocol milestones (#172), case chronology CSV (#173), filtered case audit register (#174), and protocol-finalization consequence guidance (#175). Recent throughput includes several bounded Medium, test-backed frontend/domain slices. `gh pr list --state open` returned no open PRs, so overlap risk is low. This supports one focused Medium correction to the protocol export contract, but not a schema-wide event ledger.

## 14-day strategic portfolio check

- **User-facing vs internal:** The portfolio is predominantly user-facing legal-workflow confidence, readiness, chronology, and portable artifact work; internal reliability changes directly supported trusted persistence or export behavior.
- **Size mix:** Early runs were mostly S-sized safeguards and scan signals. The later portfolio deliberately stepped up to bounded Medium audit summaries, chronology, CSV handoffs, and protocol-finalization guidance.
- **Repeated surfaces:** Cases and Vault have been heavily optimized. Yesterday touched protocol final review; today's adjacent PDF is justified because it corrects what the consequential exported artifact claims, rather than adding another review-panel detail.
- **Repeated strategic theme:** The append-only audit ledger remains repeatedly backlogged. It is still unsafe without event integrity, transaction, RLS, privacy, retention, deletion, idempotency, and backfill decisions. A source-bound export contract is a concrete prerequisite.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | user-visible impact | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| S — Remove the unsupported compliance score from the protocol PDF | 8 | 10 | 8 | 2 | 10 | 8 | BACKLOG |
| M — Replace the fabricated audit PDF with an evidence-derived protocol record | 10 | 8 | 9 | 4 | 9 | 10 | APPROVED |
| L — Persist an append-only cross-workflow audit activity ledger | 10 | 3 | 10 | 8 | 6 | 10 | BACKLOG |

## Approved

### APPROVED — Replace the fabricated audit PDF with an evidence-derived protocol record

**User-impact note:** The target user is a Swiss owner, contractor, project administrator, or adviser downloading the finalized protocol for a handoff or legal dossier. The workflow moment is immediately after finalization, when the artifact becomes the portable representation of the captured record. A real target user will notice and benefit because the PDF will stop asserting a fixed `98%`, `12/12`, and canned compliance outcomes and will instead show factual project, party, linked-case, defect, signature, and finalization state. This is not incremental polish: it removes false legal confidence from a core exported artifact.

**Explicit engineering task:** On branch `pipeline/2026-07-29-evidence-derived-protocol-pdf`, replace the hard-coded score/legal-check presentation in `components/dashboard/AuditReportPDF.tsx` with a typed, evidence-derived finalized-protocol summary. Pass the actual defect/no-defect statement and signature-capture state from `app/dashboard/page.tsx`; preserve project, date, linked case, contractor, client, download behavior, and legal-advice disclaimer. Add a pure report-summary contract and focused unit/integration coverage. Do not change Supabase persistence, checklist sync, PDF storage, signature cryptography, deadline math, routes, finalization locks, or database schema.

## Backlogged

### BACKLOG — Remove the unsupported compliance score from the protocol PDF

This is safe and valuable, but the approved Medium slice both removes the false claim and replaces it with useful source-bound facts. Keep the S slice as an emergency fallback only if the typed report contract proves unexpectedly unsafe.

### BACKLOG — Persist an append-only cross-workflow audit activity ledger

This remains the highest strategic infrastructure direction, but is not safe as one autonomous PR. Its smallest credible phase requires an explicit event/RLS/retention contract and an atomic, idempotent `protocol.finalized` write while retaining source-derived chronology compatibility.

## Rejected

None.

## Engineering result

Implemented on `pipeline/2026-07-29-evidence-derived-protocol-pdf`:

- Added a typed, pure finalized-protocol report contract that distinguishes documented defects, explicit no-visible-defects confirmation, and missing evidence without inventing UI claims.
- Captured that report snapshot only after successful finalization and reused the immutable snapshot for PDF download, so later refreshes cannot drift the exported evidence away from the finalized workflow state.
- Replaced the fixed `98%`, `12/12`, `COMPLIANT`, and `RECOMMENDATION` output with factual protocol status, record/case identifiers, parties, defect evidence, and signature-capture state.
- Preserved the existing persistence, linked-case checklist sync, PDF download lifecycle, and legal-advice disclaimer.

## Product review after implementation

The shipped slice matches the approved user-impact note. A user downloading the finalized record no longer receives fabricated compliance scoring or canned legal outcomes; the artifact now reports only source-bound facts captured by the finalized workflow. This is a visible legal-trust correction, not internal cleanup. Supabase persistence, checklist sync, deadline math, routing, finalization locks, signature cryptography, and database schema are unchanged.

## Validation

Passed:

- Red phase: `npm run test -- __tests__/protocol-report.test.ts` failed because the report contract did not exist.
- Red phase: `npm run test -- __tests__/dashboard-linked-case-retry.test.tsx -t "evidence-derived protocol report"` failed because the dashboard did not pass report evidence to PDF generation.
- Focused: `npm run test -- __tests__/protocol-report.test.ts __tests__/audit-report-pdf.test.tsx __tests__/dashboard-linked-case-retry.test.tsx` — 3 files / 28 tests passed.
- `npm run test` — 33 files / 276 tests passed.
- `npm run lint` — passed.
- `npm run build` — passed.
- `git diff --check` — passed.

## Review gates

- **Spec compliance: PASS** — exactly the approved evidence-derived PDF slice was implemented; unsupported fixed claims are absent, factual defect/signature/case/finalization evidence is present, and excluded persistence/routing/deadline/schema surfaces are unchanged.
- **Code quality: APPROVED** — defect evidence is an exhaustive discriminated union, whitespace is normalized, missing evidence is represented honestly, the finalization snapshot prevents export drift, component coverage asserts fabricated claims are absent, and dashboard integration coverage proves finalized state reaches PDF generation.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/176
