# Daily Product Improvement Decision — 2026-07-19

## 7-day merged-PR calibration

Recent first-parent merged PRs in the last 7 days:

- #165 `pipeline/2026-07-18-linked-case-checklist-normalization` — S linked-case checklist normalization after protocol finalization.
- #164 `pipeline/2026-07-17-case-protocol-snapshot` — S scan-level linked protocol count in case snapshots.
- #163 `pipeline/2026-07-16-case-reminder-badge` — S scan-level case reminder readiness badge.
- #162 `pipeline/2026-07-15-vault-archive-feedback` — S Vault archive/restore confirmation.
- #161 `pipeline/2026-07-14-linked-protocol-follow-up` — S dashboard linked protocol follow-up handoff.
- #160 `pipeline/2026-07-13-vault-audit-snapshot` — S Vault legal next-action/deadline snapshot.
- #159 `pipeline/2026-07-12-public-calculator-ics-feedback` — S public Ruegefrist `.ics` success/error feedback.

Calibration: recent throughput strongly supports one focused frontend product slice with existing helpers, localized copy reuse, focused regression coverage, and full `npm run test`, `npm run lint`, and `npm run build` validation. The week remains S-sized, but the work is user-facing and legally meaningful. Today should avoid cosmetic polish and continue toward the audit-cockpit theme by completing a missing scan-level readiness signal.

Open PR overlap: `gh pr list --state open` returned no open PRs, so there is no known active branch overlap.

## 14-day strategic portfolio check

- **User-facing workflow improvements vs internal fixes:** Recent work is dominated by visible legal workflow trust: reminder export feedback/idempotency, dashboard protocol-to-case handoffs, Vault audit snapshots, archive/restore confirmation, scan-level reminder/protocol state, and checklist normalization. Internal reliability changes generally support visible legal readiness.
- **S vs M vs L/phase-1 strategic choices:** Mostly S-sized slices, with repeated bounded phase-1 movement toward a unified audit/activity cockpit.
- **Repeated surfaces:** Cases, dashboard protocol finalization, Vault, reminders, and public calculators have all been touched. Another Cases slice is justified only if it completes a missing audit-critical dimension rather than restyling the same card.
- **Repeated strategic themes:** Unified audit/activity timeline remains repeatedly backlogged. The next prerequisite is making every case card expose the core audit dimensions — next action, deadline, reminders, linked protocols, and evidence readiness — before deriving a broader timeline.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Show evidence readiness in the case action snapshot | 8 | 10 | 9 | 2 | 10 | APPROVED |
| M — Add a first read-only case audit cockpit row | 9 | 6 | 10 | 5 | 8 | BACKLOG |
| L — Build a unified audit-ready activity timeline | 10 | 3 | 10 | 8 | 5 | BACKLOG |

## Approved

### APPROVED — Show evidence readiness in the case action snapshot

**User-impact note:** Target users are Swiss construction owners, contractors, and project admins scanning the Cases ledger. The workflow moment is deciding whether a defect case already has the proof needed for a legally credible notice/protocol trail. This is not just incremental polish: evidence completeness is a core audit-readiness signal, and a best-in-market compliance product should show missing proof beside deadline, reminder, and protocol state before users open details.

**Engineering task:** In `app/dashboard/cases/page.tsx`, add a dedicated localized evidence-readiness cell to the scan-level `cases-action-snapshot-*` grid using the already normalized checklist state. Reuse existing `cases-evidence-complete` and `cases-evidence-incomplete` translations where possible. Update focused coverage in `__tests__/cases-checklist-persistence.test.tsx` to assert the scan-level snapshot shows evidence readiness. Run `npm run test -- __tests__/cases-checklist-persistence.test.tsx`, `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Add a first read-only case audit cockpit row

Strategically stronger and valuable, but broader than today because it needs a concise derived summary contract and layout decisions across reminders, protocols, evidence, Vault, and deadlines. Today's approved evidence cell completes one missing input for that future row.

### BACKLOG — Build a unified audit-ready activity timeline

Strategically strongest, but too broad for one daily PR. The smallest credible phase-1 remains a read-only derived timeline helper for one case surface after scan-level audit signals are complete and stable.

## Rejected

None.

## Result

Implemented on branch `pipeline/2026-07-19-case-evidence-readiness-summary`:

- Case action snapshots now show a dedicated localized evidence-readiness cell beside next action, deadline countdown, linked protocols, and reminder readiness.
- The cell uses the existing normalized checklist state, so partial persisted checklists still preserve timeline-derived defaults before displaying proof readiness.
- Added focused regression coverage proving the scan-level snapshot exposes incomplete evidence state.

## Product review after implementation

The shipped slice matches the approved user-impact note: a user scanning the Cases ledger can immediately see whether legal proof is still missing before relying on reminders, protocols, or next-action guidance. The change is read-only, localized, and does not alter checklist persistence, Vault routing, protocol creation, deadline math, reminders, or URL state.

## Validation

Passed:

- `npm run test -- __tests__/cases-checklist-persistence.test.tsx` — passed (11 tests).
- `npm run test` — passed (31 files, 249 tests).
- `npm run lint` — passed.
- `npm run build` — passed.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/166
