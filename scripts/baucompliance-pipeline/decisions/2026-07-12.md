# Daily Product Improvement Decision — 2026-07-12

## 7-day merged-PR calibration

Recent first-parent merged PRs in the last 7 days:

- #158 `pipeline/2026-07-11-case-reminder-export-feedback` — S case reminder export success/error feedback.
- #157 `pipeline/2026-07-10-linked-case-checklist-sync` — S linked protocol finalization checklist sync correctness.
- #156 `pipeline/2026-07-09-deadline-ics-feedback` — S dashboard deadline `.ics` export feedback.
- #155 `pipeline/2026-07-08-case-calendar-export-idempotent` — S case reminder export idempotency.
- #154 `pipeline/2026-07-07-protocol-final-review` — bounded M/S protocol final-review summary.
- #153 `pipeline/2026-07-06-missing-case-handoff-feedback` — S dashboard protocol handoff feedback.
- #152 `pipeline/2026-07-05-vault-readiness-summary` — S vault readiness explainability.

Calibration: recent throughput supports one focused frontend product-confidence slice with focused tests plus full test/lint/build validation. The week has been mostly S-sized, but these have not been cosmetic: they tightened legal artifact confidence, readiness state, and cross-workflow handoffs. Today's safest high-leverage continuation is to apply the same export-feedback reliability to the public calculator, where a silent reminder export affects conversion and legal trust.

Open PR overlap: `gh pr list` returned no open PRs, so there is no known active overlap.

## 14-day strategic portfolio check

- **User-facing workflow improvements vs internal fixes:** The portfolio is dominated by user-facing legal workflow reliability: public calculator/case handoffs, reminder exports, protocol final review, vault readiness, URL-state trust, and linked-case synchronization. Internal work has generally served visible legal-confidence outcomes.
- **S vs M vs L/phase-1 strategic choices:** Mostly S-sized daily slices, with bounded Medium/phase-1 work around protocol review and case/vault context. This is safe for daily shipping, but the product should continue moving toward audit-ready summaries.
- **Repeated surfaces:** Cases, reminders, deadlines, dashboard protocol handoffs, and vault have all been touched repeatedly. The approved public calculator slice is justified because it closes a visible gap on the acquisition/activation surface using the same artifact-feedback contract already established inside the dashboard.
- **Repeated strategic backlog:** Unified audit/activity timeline remains the strongest L-sized theme, but the prerequisite is reliable, explicit artifact-action state on each surface. The public calculator export remains one such gap.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Confirm public Ruegefrist calendar downloads | 8 | 10 | 8 | 2 | 10 | APPROVED |
| M — Add a first audit-activity summary card to Vault project rows | 8 | 6 | 9 | 5 | 7 | BACKLOG |
| L — Build a unified audit-ready activity timeline across cases, protocols, vault evidence, and reminders | 10 | 3 | 10 | 8 | 5 | BACKLOG |

## Approved

### APPROVED — Confirm public Ruegefrist calendar downloads

**User-impact note:** Target users are Swiss construction owners, contractors, and project admins using the public Ruegefrist calculator at the moment they export a legally relevant 60-day reminder. This is not incremental polish: a silent `.ics` export can leave users uncertain whether a deadline-protection artifact was actually prepared. Localized success/error feedback makes the legal reminder workflow more trustworthy and aligns the public calculator with dashboard export-confidence behavior.

**Engineering task:** In `components/ruegefrist-calculator.tsx`, add temporary success/error feedback around `downloadICS`, clear stale feedback when dates, reminder presets, reset, or unmount invalidate the exported artifact, add focused coverage in `__tests__/ruegefrist-calculator.test.tsx`, and add localized keys in `locales/index.ts`. Run `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Add a first audit-activity summary card to Vault project rows

Valuable Medium follow-up toward the recurring audit-ready cockpit theme, but broader than today's public export-feedback gap because it needs a concise copy/display contract for multiple artifact states.

### BACKLOG — Build a unified audit-ready activity timeline across cases, protocols, vault evidence, and reminders

Strategically strongest, but too broad for one daily PR. The smallest credible future phase-1 is a read-only timeline from existing confirmed case/protocol/checklist/reminder state after all artifact actions expose reliable success/error outcomes.

## Rejected

None.

## Product review after implementation

The shipped slice still matches the approved user-impact note: the public Ruegefrist calculator now confirms successful `.ics` preparation and shows retry feedback when calendar-file preparation fails. Feedback is cleared when dates, reminder presets, reset, or unmount make the prior export stale. The change is visible in the public legal-deadline workflow and does not alter deadline math, reminder serialization, share links, or case handoff URLs.

## Validation

Passed:

- `npm run test -- __tests__/ruegefrist-calculator.test.tsx` — passed (20 tests).
- `npm run test` — passed (31 files, 237 tests).
- `npm run lint` — passed.
- `npm run build` — passed.

## PR link

Pending PR creation.
