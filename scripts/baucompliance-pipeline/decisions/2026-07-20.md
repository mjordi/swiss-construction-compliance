# Daily Product Improvement Decision — 2026-07-20

## 7-day merged-PR calibration

Recent first-parent merged PRs in the last 7 days:

- #166 `pipeline/2026-07-19-case-evidence-readiness-summary` — S scan-level evidence readiness in case action snapshots.
- #165 `pipeline/2026-07-18-linked-case-checklist-normalization` — S linked-case checklist normalization after protocol finalization.
- #164 `pipeline/2026-07-17-case-protocol-snapshot` — S scan-level linked protocol count in case snapshots.
- #163 `pipeline/2026-07-16-case-reminder-badge` — S scan-level case reminder readiness badge.
- #162 `pipeline/2026-07-15-vault-archive-feedback` — S Vault archive/restore confirmation.
- #161 `pipeline/2026-07-14-linked-protocol-follow-up` — S dashboard linked protocol follow-up handoff.
- #160 `pipeline/2026-07-13-vault-audit-snapshot` — S Vault legal next-action/deadline snapshot.

Calibration: recent throughput supports one focused frontend product slice with localized copy, focused regression coverage, and full `npm run test`, `npm run lint`, and `npm run build` validation. The portfolio remains S-sized, but the work is user-facing and legally meaningful. Today should not start the full audit cockpit until the remaining scan-level readiness contract is complete.

Open PR overlap: `gh pr list --state open` returned no open PRs, so there is no known active branch overlap.

## 14-day strategic portfolio check

- **User-facing workflow improvements vs internal fixes:** Recent work is dominated by user-facing legal workflow trust: public and dashboard reminder feedback, protocol/case handoffs, Vault audit context, archive/restore confirmation, scan-level reminder/protocol/evidence state, and checklist normalization.
- **S vs M vs L/phase-1 strategic choices:** Mostly S-sized slices, each acting as a prerequisite for the repeated audit-cockpit theme.
- **Repeated surfaces:** Cases has been touched often, but the pattern has been completing distinct legal readiness dimensions rather than cosmetic repetition.
- **Repeated strategic themes:** A unified audit/activity timeline remains repeatedly backlogged. The next prerequisite is making every case card expose core readiness dimensions: next action, deadline, protocols, evidence, notice draft, and reminders.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Show notice draft readiness in the case action snapshot | 8 | 10 | 9 | 2 | 10 | APPROVED |
| M — Add a first read-only case audit summary row | 9 | 6 | 10 | 5 | 8 | BACKLOG |
| L — Build a unified audit-ready activity timeline | 10 | 3 | 10 | 8 | 5 | BACKLOG |

## Approved

### APPROVED — Show notice draft readiness in the case action snapshot

**User-impact note:** Target users are Swiss construction owners, contractors, and project admins scanning the Cases ledger. The workflow moment is deciding whether a defect case is ready to become a legally reliable notice package. This is not just incremental polish: missing notice drafts are a legal-action blocker, and a best-in-market compliance product should show notice readiness beside deadline, protocol, evidence, and reminder state before users open details.

**Engineering task:** In `app/dashboard/cases/page.tsx`, add a localized `cases-notice-readiness` cell to the scan-level `cases-action-snapshot-*` grid using the already normalized `checklist.noticeDrafted` state. Add localized ready/pending copy in `locales/index.ts` and focused coverage in `__tests__/cases-checklist-persistence.test.tsx` proving the snapshot exposes pending notice draft readiness. Run `npm run test -- __tests__/cases-checklist-persistence.test.tsx`, `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Add a first read-only case audit summary row

Strategically stronger than a single cell, but broader than today because it needs a concise derived priority/copy contract across deadlines, protocols, evidence, notice readiness, and reminders. Today's approved notice-readiness cell completes a missing input for that future summary.

### BACKLOG — Build a unified audit-ready activity timeline

Strategically strongest, but too broad for one daily PR. The smallest credible phase-1 remains a read-only derived timeline/helper for one case surface after scan-level audit signals are complete and stable.

## Rejected

None.

## Result

Implemented on branch `pipeline/2026-07-20-case-notice-readiness-snapshot`:

- Case action snapshots now show a dedicated localized notice-readiness cell beside next action, deadline countdown, linked protocols, evidence readiness, and reminder readiness.
- The cell uses the existing normalized `checklist.noticeDrafted` state, so partial persisted checklists still preserve timeline-derived defaults before displaying notice readiness.
- Added focused regression coverage proving the scan-level snapshot exposes pending notice draft readiness.

## Product review after implementation

The shipped slice matches the approved user-impact note: a user scanning the Cases ledger can immediately see whether the notice draft/package is still pending before relying on proof, protocols, reminders, and deadline context. The change is read-only, localized, and does not alter checklist persistence, Vault routing, protocol creation, deadline math, reminders, or URL state.

## Validation

Passed:

- `npm run test -- __tests__/cases-checklist-persistence.test.tsx` — passed (12 tests).
- `npm run test` — passed (31 files, 250 tests).
- `npm run lint` — passed.
- `npm run build` — passed.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/167
