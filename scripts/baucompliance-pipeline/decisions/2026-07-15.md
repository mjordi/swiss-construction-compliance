# Daily Product Improvement Decision — 2026-07-15

## 7-day merged-PR calibration

Recent first-parent merged PRs in the last 7 days:

- #161 `pipeline/2026-07-14-linked-protocol-follow-up` — S linked-protocol success-state follow-up into Cases.
- #160 `pipeline/2026-07-13-vault-audit-snapshot` — S Vault legal next-action/deadline snapshot.
- #159 `pipeline/2026-07-12-public-calculator-ics-feedback` — S public Ruegefrist `.ics` success/error feedback.
- #158 `pipeline/2026-07-11-case-reminder-export-feedback` — S case reminder export success/error feedback.
- #157 `pipeline/2026-07-10-linked-case-checklist-sync` — S linked protocol finalization checklist sync correctness.
- #156 `pipeline/2026-07-09-deadline-ics-feedback` — S dashboard deadline `.ics` export feedback.
- #155 `pipeline/2026-07-08-case-calendar-export-idempotent` — S case reminder export idempotency.

Calibration: recent throughput supports one focused frontend product slice with localized UI copy, a focused regression test, and full `npm run test`, `npm run lint`, and `npm run build` validation. The portfolio remains mostly S-sized, but it is user-facing and legally meaningful. Today should keep advancing the Vault/audit-readiness theme without taking on the full L-sized activity timeline.

Open PR overlap: `gh pr list --state open` returned no open PRs, so there is no known active branch overlap.

## 14-day strategic portfolio check

- **User-facing workflow improvements vs internal fixes:** Dominated by user-facing legal workflow confidence: reminder export confirmations, case/protocol handoffs, Vault readiness and audit snapshots, linked-case checklist sync, and URL-state reliability. Internal fixes generally supported visible trust outcomes.
- **S vs M vs L/phase-1 strategic choices:** Mostly S-sized daily slices, with bounded Medium/phase-1 movement around protocol final review and cross-workflow handoffs. A focused Vault archive/restore confirmation is still acceptable because it addresses a legal-state transition, not cosmetic copy.
- **Repeated surfaces:** Cases, dashboard protocol, public calculator, deadlines, reminders, and Vault have all been touched. Vault is worth touching again because the last Vault slice surfaced legal context; today’s slice makes a Vault lifecycle mutation explicit.
- **Repeated strategic themes:** Unified audit/activity timeline remains repeatedly backlogged. The next prerequisite is making existing state transitions explicit and trustworthy before introducing a broader timeline model.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Confirm Vault archive and restore actions inline | 7 | 10 | 8 | 2 | 10 | APPROVED |
| M — Add a first Vault archive activity summary from existing case/protocol state | 8 | 7 | 9 | 5 | 8 | BACKLOG |
| L — Build a unified audit-ready activity timeline across cases, protocols, vault evidence, and reminders | 10 | 3 | 10 | 8 | 5 | BACKLOG |

## Approved

### APPROVED — Confirm Vault archive and restore actions inline

**User-impact note:** Target users are Swiss construction owners, contractors, and project admins curating the Technical Vault. The workflow moment is archiving an evidence project after a legal follow-up is complete, or restoring it for renewed review. This is not just incremental polish: archive/restore changes whether a project appears in active legal review or later audit review, and a best-in-market compliance tool should confirm that the legal evidence lifecycle transition persisted.

**Engineering task:** In `app/dashboard/vault/page.tsx`, add per-project localized success feedback after archive/restore persistence succeeds, clear stale success when the project is mutated again, preserve current rollback/error behavior, add localized keys in `locales/index.ts`, and add focused coverage in `__tests__/vault-follow-up-link.test.tsx`. Run `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Add a first Vault archive activity summary from existing case/protocol state

Valuable Medium follow-up toward the audit cockpit, but broader than today because it needs a stronger copy/layout contract for activity priority and must avoid duplicating existing next-action/deadline/protocol signals.

### BACKLOG — Build a unified audit-ready activity timeline across cases, protocols, vault evidence, and reminders

Strategically strongest, but too broad for one daily PR. The smallest credible future phase-1 remains a read-only derived timeline for one case/Vault card after existing state transitions consistently expose user-visible confirmations.

## Rejected

None.

## Result

Implemented on branch `pipeline/2026-07-15-vault-archive-feedback`:

- Technical Vault now shows a localized confirmation banner after archive persistence succeeds.
- Technical Vault now shows a localized confirmation banner after restore persistence succeeds.
- Stale success feedback for the same project is cleared before a retry/new mutation and when that mutation fails, preserving the existing rollback/error behavior.

## Product review after implementation

The shipped slice matches the approved user-impact note: archive/restore is a visible legal evidence lifecycle transition, and the product now confirms that the transition persisted even when the card immediately moves between active and archived tabs. The change is user-visible, localized, and does not alter Vault routing, case timeline derivation, persistence schema, protocol counts, checklist progress, or deadline logic.

## Validation

Passed:

- `npm run test -- __tests__/vault-follow-up-link.test.tsx` — passed (17 tests).
- `npm run test` — passed (31 files, 242 tests).
- `npm run lint` — passed.
- `npm run build` — passed.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/162
