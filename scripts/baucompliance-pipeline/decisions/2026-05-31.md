# Daily Product Improvement Decision — 2026-05-31

## 7-day calibration

First-parent merged work in the last 7 days includes PRs #109, #110, #111, #112, #113, #114, #115, and #116. The successful cadence supports one narrow, reviewable UI reliability fix with focused regression coverage and full `test`/`lint`/`build` validation. There are no open PRs at approval time, so overlap risk is low. Recent throughput should be used as a safety governor rather than a reason to avoid meaningful Medium work, but today's safest high-leverage slice is the localized settings race fix.

## PM scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
|---|---:|---:|---:|---:|---:|---|
| S — Preserve settings edits made while profile save is in flight | 7 | 9 | 7 | 3 | 9 | APPROVED |
| M — Add accessible settings form labels and pending-state lockout | 7 | 7 | 7 | 4 | 7 | BACKLOG |
| L — Account activity and security event timeline | 8 | 3 | 8 | 8 | 4 | BACKLOG |

## Rationale

- **Approved:** The Small proposal addresses a concrete data-loss/confusing-feedback race on an account-management surface. It is localized, testable, and calibrated to the recent daily reliability-fix merge size.
- **Backlogged:** The Medium accessibility proposal is valuable, but it broadens the settings surface area and should be handled as its own focused pass after the async-save contract is safe.
- **Backlogged:** The Large account activity timeline is strategically relevant to compliance/trust, but too broad for one unattended daily PR without product and schema design.
- **Rejected:** None.

## Engineering task

Implement the approved Small proposal:

1. In `app/dashboard/settings/page.tsx`, track the latest visible profile form state during saves.
2. After a profile save succeeds, update the loaded snapshot, but do not overwrite `fullName`/`company` or show the saved success state if the user has already edited away from the submitted snapshot.
3. Add a focused component regression test that starts a profile save, edits the name before the mocked update resolves, then verifies the newer edit remains visible and the save button remains available for the unsaved edit.
4. Run required validation: `npm run test`, `npm run lint`, and `npm run build`.

## Validation

- `npm run test -- __tests__/settings-profile-save-race.test.tsx` — passed (1 test).
- `npm run test -- __tests__/settings-profile-save-race.test.tsx __tests__/settings-password-feedback.test.tsx __tests__/settings.test.ts` — passed (3 files, 8 tests).
- `npm run test` — passed (26 files, 164 tests).
- `npm run lint` — passed.
- `npm run build` — passed.

## PR

PR link: https://github.com/mjordi/swiss-construction-compliance/pull/117
