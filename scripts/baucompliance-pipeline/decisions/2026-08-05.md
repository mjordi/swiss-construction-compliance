# Daily Product Improvement Decision — 2026-08-05

## 7-day merged-PR calibration

After fetching `origin/main`, first-parent history shows six focused product merges in the last seven days: evidence-derived protocol PDF (#176), case audit dossier PDF (#177), finalized-protocol retrieval (#178), private case evidence/Vault storage (#179), corrected legal deadline basis (#180), and trigger-derived evidence upload activity (#181). The recent portfolio supports one bounded Medium that reuses established schema and chronology contracts without adding another migration. GitHub reports no open PRs, so overlap risk is low.

## 14-day strategic portfolio check

- **User-facing vs internal:** Work is overwhelmingly user-facing legal confidence, source-record retrieval, chronology, exports, deadline correctness, private evidence, and provenance. Reliability/security hardening directly protected those outcomes.
- **Size mix:** The portfolio has progressed from S-sized readiness prerequisites into repeated bounded Medium and strategic phase-1 slices; it is not trapped at Small.
- **Repeated surfaces:** Cases and Vault are heavily optimized. Another badge, copy tweak, or independent export would be marginal.
- **Strategic themes:** The append-only audit/activity direction is no longer abstract: yesterday created the first source-bound event, but it remains visible only in the Vault. Composing it into the established Case chronology is the smallest credible cross-workflow phase before a source-bound notice preview.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | user-visible impact | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| S — Add password recovery to sign-in | 8 | 8 | 7 | 3 | 9 | 8 | BACKLOG |
| M — Add source-bound evidence uploads to the Case chronology | 10 | 8 | 10 | 4 | 10 | 9 | APPROVED |
| L — Generate and track a source-bound defect notice package | 10 | 2 | 10 | 9 | 7 | 10 | BACKLOG |

## Approved

### APPROVED — Add source-bound evidence uploads to the Case chronology

**User-impact note:** The target user is a Swiss owner, contractor, project administrator, compliance lead, or adviser reviewing a defect case before notice, escalation, or external handoff. The workflow moment is reconstructing when source evidence entered the dossier. A real user will notice because expanded Case details and the existing chronology export will identify factual, database-recorded evidence uploads without requiring a Vault detour. This is not incremental polish: it turns the first persisted activity-ledger event into a cross-workflow audit record.

**Explicit engineering task:** On branch `pipeline/2026-08-05-case-evidence-chronology`, extend `lib/case-timeline.ts` with a typed evidence-upload chronology input and milestone carrying source ID/name; query owner-scoped `case_activity_events` in `app/dashboard/cases/page.tsx` while isolating activity failures from the established Cases/protocol load; render localized `evidence uploaded` milestones in deterministic order; and include evidence IDs/names in `buildCaseLegalChronologyCsv`. Update all four locales and focused domain/UI tests, including activity-load failure fallback. Do not add schema changes, event writes, generic event types, evidence download/delete actions in Cases, notice generation, reminder delivery, or changes to legal deadline/checklist/protocol/Vault behavior.

## Backlogged

### BACKLOG — Add password recovery to sign-in

A meaningful activation/access improvement and a safe fallback, but it has less strategic compounding value than making newly established legal provenance usable in the main review workflow.

### BACKLOG — Generate and track a source-bound defect notice package

This is the strongest end-to-end outcome, but recipient authority, content review, delivery channels, receipts, and legal claims remain unsafe in one daily PR. The smallest credible next phase is a typed, explicitly non-sending notice preview sourced from the case chronology after evidence provenance is proven across surfaces.

## Rejected

None.

## Engineering result

Implemented on `pipeline/2026-08-05-case-evidence-chronology`:

- Extended the shared legal chronology contract with typed, source-bound `evidence-uploaded` milestones carrying immutable evidence IDs and factual source names.
- Loaded owner-scoped activity independently from the established Cases/protocol requests. Slow, failed, stale, or cross-user activity responses cannot hide core case content or reuse another user's fallback provenance.
- Rendered localized evidence-upload milestones and source filenames in expanded Case chronology, with Swiss-calendar machine-readable dates.
- Extended the existing chronology CSV with generic source ID and source-file columns so protocol and evidence provenance share one deterministic, spreadsheet-safe handoff.
- Added DE/FR/IT/EN copy plus focused domain/UI coverage for ordering, invalid timestamps, Swiss-day formatting, export arguments/content, activity errors, and indefinitely pending activity loads.

## Product review after implementation

The shipped slice matches the approved user-impact note. A user reviewing a defect case can now see exactly when a source evidence file was recorded and identify that file without switching to the Vault; the same factual provenance travels in the existing chronology CSV. This composes the first persisted activity event into the legal-review workflow rather than adding another badge or internal cleanup. Schema, event writes, generic event types, evidence mutation/download actions in Cases, notice generation, reminder delivery, legal deadline math, checklist persistence, protocol behavior, and Vault behavior remain unchanged.

## Validation

Passed:

- Red phase: `npm run test -- __tests__/case-timeline.test.ts -t "source-bound evidence uploads"` failed because the chronology ignored evidence events.
- Focused: `npm run test -- __tests__/case-timeline.test.ts __tests__/cases-checklist-persistence.test.tsx __tests__/locales.test.ts` — 3 files / 59 tests passed after implementation and review hardening.
- Required `npm run test` — 41 files / 375 tests passed.
- Required `npm run lint` — passed with no errors or warnings.
- Required `npm run build` — passed; TypeScript completed and all 13 static pages generated.
- `git diff --check` — passed.

## Review gates

- **Spec compliance: PASS** — exactly one read-only `evidence_uploaded` event type is added to Case chronology and CSV, using owner-scoped source records and all four locales; every excluded persistence/schema/notice/deadline/checklist/protocol/Vault surface is unchanged.
- **Code quality/security: APPROVED after review fixes** — activity fetching no longer blocks core case rendering, returned errors and thrown/indefinitely pending requests are isolated, stale fetch IDs are rejected, fallback activity is user-bound, source names/IDs use existing CSV formula neutralization, and rendered machine dates match the Swiss calendar day.
- **Final integration:** A final fetch found `origin/main` unchanged at `4b0df1c`, and GitHub still reported no open PRs, so there is no fresh overlap.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/182
