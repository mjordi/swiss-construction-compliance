# Daily Product Improvement Decision — 2026-07-27

## 7-day merged-PR calibration

After fetching `origin/main`, first-parent history since 2026-07-20 shows six focused product merges: case notice readiness (#167), case audit readiness (#168), Vault audit blockers (#170), legal milestone timeline (#171), linked protocol milestones (#172), and case chronology CSV (#173), plus weekly security maintenance. Recent work progressed from S-sized scan signals to bounded Medium chronology/export outcomes; that throughput supports one more focused Medium slice reusing existing derived contracts, but not a schema-wide event ledger. `gh pr list --state open` returned no open PRs, so overlap risk is low.

## 14-day strategic portfolio check

The portfolio is predominantly user-facing legal-workflow improvement rather than internal-only maintenance. Most early choices were S-sized reliability and scan-level readiness slices; the last week stepped into bounded Medium audit context, chronology, and export work. Cases and Vault are repeated, heavily optimized surfaces. The append-only audit ledger remains repeatedly backlogged, but its migration, atomicity, RLS, retention, and idempotency contract is not safe to improvise in one run. Today should avoid another isolated card detail and instead turn existing readiness signals into a portfolio-level handoff.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Show the visible-case count in the shared-view action label | 4 | 10 | 4 | 1 | 10 | REJECTED |
| M — Export the filtered case audit register | 9 | 8 | 9 | 4 | 9 | APPROVED |
| L — Persist an append-only cross-workflow audit ledger | 10 | 3 | 10 | 8 | 6 | BACKLOG |

## Approved

### APPROVED — Export the filtered case audit register

**User-impact note:** The target user is a Swiss owner, contractor, project administrator, compliance lead, or adviser preparing a portfolio triage or audit handoff. The workflow moment is after filtering/sorting the Cases ledger and before sharing the selected case set outside BauCompliance. This is not incremental polish: it turns legal status, deadline, checklist, protocol, and audit-readiness signals into a portable portfolio artifact and replaces manual spreadsheet compilation.

**Engineering task:** On branch `pipeline/2026-07-27-filtered-case-audit-register`, add a pure deterministic localized CSV serializer in `lib/case-timeline.ts`; export exactly `visibleCases` in their current order with generated-at metadata, case/project/canton, regime/status, notice deadline, checklist progress, linked-protocol count, and audit-readiness progress; add a localized Cases action with a deterministic filename; preserve UTF-8 BOM, quoting, and spreadsheet-formula neutralization; add focused domain and Cases UI tests and update affected mocks. Do not change persistence, deadline math, checklist mutations, filters/URL behavior, per-case chronology, routes, or database schema. Run `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Persist an append-only cross-workflow audit ledger

Strategically strongest, but unsafe as one daily PR. Its smallest credible phase-1 requires an explicit event contract and an atomic/idempotent database write for `protocol.finalized`, including RLS and retry semantics, while retaining the existing source-derived chronology fallback.

## Rejected

### REJECTED — Show the visible-case count in the shared-view action label

Safe and visible, but it only clarifies existing copy and would continue the pattern of marginal card/action polish. It does not create the portfolio audit handoff delivered by the approved Medium slice.

## Result

Implemented on branch `pipeline/2026-07-27-filtered-case-audit-register`:

- Added a pure deterministic audit-register CSV serializer that preserves the supplied visible-case order.
- The register includes generated-at metadata, case/project/canton, localized legal regime and status, notice deadline, effective checklist progress, linked-protocol count, and overall audit-readiness progress.
- Old-law rows leave the fixed notice-deadline field empty and count the non-applicable calendar requirement as audit-ready.
- Every field is quoted; formula-leading text is neutralized; legal dates use the Swiss calendar day.
- The Cases filter area now offers a localized export action that serializes exactly the current `visibleCases` set and cleans up its object URL without mutating persisted state.
- Added focused domain and UI regression coverage in addition to DE/FR/IT/EN localization.

## Product review after implementation

The shipped slice matches the approved user-impact note. A Swiss owner, contractor, project administrator, compliance lead, or adviser can now turn the filtered/sorted Cases view into a portable portfolio register without manually transcribing legal status and readiness signals. This creates a new audit-handoff outcome rather than another isolated card polish item. Persistence, deadline math, checklist mutation behavior, URL filters, per-case chronology, routes, and database schema remain unchanged.

## Validation

Passed:

- Red phase: `npm run test -- __tests__/case-timeline.test.ts -t "case audit register CSV"` failed before the serializer existed.
- Red phase: `npm run test -- __tests__/cases-checklist-persistence.test.tsx -t "filtered audit register"` failed before the export action existed.
- Focused domain/UI: 2 files / 35 tests passed.
- First full `npm run test` exposed missing English localization parity; the English keys were added and `npm run test -- __tests__/locales.test.ts` then passed (12 tests).
- Final `npm run test` — 31 files / 264 tests passed.
- `npm run lint` — passed.
- `npm run build` — passed.
- `git diff --check` — passed.

Review gates:

- Spec compliance: PASS — current-view scope/order, required localized fields, read-only behavior, CSV safety, and excluded surfaces match approval.
- Code quality: APPROVED after replacing a state-changing filter test setup with stable initial URL state, eliminating React `act(...)` warnings from the new regression test, and adding the missing English locale parity found by the full suite.
- Final integration: `origin/main` remained at `5a677e5` and `gh pr list --state open` remained empty after the final fetch, so there is no fresh overlap.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/174
