# Daily Product Improvement Decision — 2026-06-13

## 7-day calibration

Recent first-parent merges on `origin/main`:

- #129 `pipeline/2026-06-12-dashboard-download-feedback` — focused dashboard async download feedback and tests.
- #128 `pipeline/2026-06-11-dashboard-label-accessibility` — small dashboard label/accessibility fix plus tests.
- #127 `pipeline/2026-06-10-settings-labels` — small settings label/accessibility fix plus tests.
- #126 `pipeline/2026-06-09-password-save-race` — focused settings async race fix.
- #125 `pipeline/2026-06-08-clear-stale-deadlines` — focused deadlines stale-results fix.
- #124 `pipeline/2026-06-07-reminder-aria-pressed` — small accessibility sweep.
- #123 `pipeline/2026-06-06-ruegefrist-reminder-presets` — bounded reminder-preset feature.

This supports approving one focused reliability/UX change today. There are no open PRs, so overlap risk is low. The inline case-edit cache fix is Medium only because it touches optimistic state semantics, but it is limited to one component and one existing test file, and it mirrors a delete-path regression pattern that already exists.

## PM scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
|---|---:|---:|---:|---:|---:|---|
| Wire login form labels and auth feedback live regions | 6 | 9 | 7 | 2 | 9 | BACKLOG |
| Keep inline case-edit cache fresh after successful updates | 8 | 9 | 8 | 3 | 9 | APPROVED |
| Add a safe client-storage helper for draft persistence | 8 | 5 | 8 | 6 | 5 | BACKLOG |

## Rationale

- **APPROVED — Keep inline case-edit cache fresh after successful updates:** This protects the core case-management workflow from stale state after a successful save and fits the recent async/optimistic reliability cadence. The implementation is narrow and testable, with an existing delete-cache regression as a template.
- **BACKLOG — Wire login form labels and auth feedback live regions:** Valuable and low risk, but today’s better product reliability win is preventing post-save data reverts on the main dashboard workflow.
- **BACKLOG — Add a safe client-storage helper for draft persistence:** Strategically useful, but too cross-cutting for today without a separate phased plan and broader storage-failure tests.

## Engineering task

On branch `pipeline/2026-06-13-case-edit-cache-refresh`:

1. Add a focused failing regression test in `__tests__/cases-inline-edit.test.tsx` that successfully edits `case-1`, queues a failed follow-up cases refresh, and verifies the updated case value remains visible with no `cases-load-error`.
2. Update `app/dashboard/cases/page.tsx` so successful inline edits update `lastSuccessfulCasesRef.current` with the same edited payload applied to visible `dbCases` before triggering the follow-up refresh/error-recovery path.
3. Run focused cases inline-edit tests, then required validation: `npm run test`, `npm run lint`, `npm run build`.

## PR

PR link: Not opened in this cron run
