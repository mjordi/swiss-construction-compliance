# Daily Product Improvement Decision — 2026-06-17

## 7-day calibration

Recent first-parent merges on `origin/main` in the last 7 days:

- #133 `pipeline/2026-06-16-cases-supabase-memo` — focused case client stability/regression test.
- #132 `pipeline/2026-06-15-vault-pending-navigation` — focused Vault pending-navigation lock.
- #131/#129 — case edit/cache refresh reliability slices.
- #130 — login label accessibility.
- #128 — dashboard download feedback.
- #127 — settings labels.

The delivery baseline is healthy but consistently small and test-focused. No open PRs were found, so overlap risk is mainly recent-surface overlap rather than active branch conflict. Today's approval should therefore be one bounded UI/navigation improvement with focused tests.

## Scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Announce Vault archive/restore pending state | 6 | 9 | 7 | 2 | 9 | BACKLOG |
| M — Add Tech Vault to shared dashboard navigation | 8 | 8 | 8 | 3 | 9 | APPROVED |
| L — Identity-based Dashboard ↔ Vault ↔ Cases handoffs | 9 | 5 | 9 | 7 | 5 | BACKLOG |

## Decisions

### APPROVED — M: Add Tech Vault to shared dashboard navigation

Rationale: Vault is already a real workflow and recent work improved its handoffs/pending state, but the primary dashboard nav still hides it. Adding it to the shared nav is a user-visible cross-workflow discoverability improvement that fits recent daily PR size and has clear test coverage.

**Engineering task:** Add a `menu-vault` localized nav item for `/dashboard/vault` in `components/dashboard/Sidebar.tsx`, ensure `MobileNav` consumes it through the shared `navItems`, and add focused RTL coverage for desktop/mobile Vault nav exposure and active title behavior.

### BACKLOG — S: Announce Vault archive/restore pending state

Useful accessibility polish, but it overlaps directly with yesterday's Vault pending navigation surface. Keep as a good future S-sized candidate after the current navigation discoverability gap is addressed.

### BACKLOG — L: Identity-based Dashboard ↔ Vault ↔ Cases handoffs

Strategically strong, but too broad for today's calibrated run. It touches URL contracts, multiple workflows, target focus behavior, and several existing tests; should be split into a phase-1 plan before implementation.

## PR

PR link: Not opened in this cron run
