# Daily Product Improvement Decision — 2026-07-14

## 7-day merged-PR calibration

Recent first-parent merged PRs in the last 7 days:

- #160 `pipeline/2026-07-13-vault-audit-snapshot` — S Vault legal next-action/deadline snapshot.
- #159 `pipeline/2026-07-12-public-calculator-ics-feedback` — S public Ruegefrist `.ics` success/error feedback.
- #158 `pipeline/2026-07-11-case-reminder-export-feedback` — S case reminder export success/error feedback.
- #157 `pipeline/2026-07-10-linked-case-checklist-sync` — S linked protocol finalization checklist sync correctness.
- #156 `pipeline/2026-07-09-deadline-ics-feedback` — S dashboard deadline `.ics` export feedback.
- #155 `pipeline/2026-07-08-case-calendar-export-idempotent` — S case reminder export idempotency.
- #154 `pipeline/2026-07-07-protocol-final-review` — bounded M/S protocol final-review summary.

Calibration: recent throughput supports one focused frontend product slice with a narrow test and full test/lint/build validation. The week is mostly S-sized, but it has been user-facing and legally meaningful. Today should keep stepping toward the recurring audit-cockpit theme with a cross-workflow bridge after protocol finalization rather than another same-surface export-feedback polish patch.

Open PR overlap: `gh pr list --state open` returned no open PRs, so there is no known active overlap.

## 14-day strategic portfolio check

- **User-facing workflow improvements vs internal fixes:** Dominated by user-facing legal workflow reliability: Vault readiness/audit snapshots, case/protocol handoffs, reminder exports, public calculator confidence, linked-case checklist sync, and URL-state trust.
- **S vs M vs L/phase-1 strategic choices:** Mostly S-sized daily slices, with bounded Medium/phase-1 moves around protocol final review and cross-workflow handoffs.
- **Repeated surfaces:** Cases, dashboard protocol, Vault, deadlines, and reminders have all been touched. The approved dashboard success-state handoff is justified because it connects existing surfaces and completes a workflow loop rather than deepening a single surface.
- **Repeated strategic themes:** Unified audit/activity timeline and legal command-center context remain repeatedly backlogged. Today's approved item is a safe prerequisite: once protocol finalization updates case readiness, users should be routed back to the case ledger where that readiness is visible.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Show a linked-case follow-up action after protocol finalization | 8 | 10 | 9 | 2 | 10 | APPROVED |
| M — Add a dashboard next legal action summary for the selected linked case | 8 | 7 | 9 | 5 | 8 | BACKLOG |
| L — Build a unified audit-ready activity timeline across cases, protocols, vault evidence, and reminders | 10 | 3 | 10 | 8 | 5 | BACKLOG |

## Approved

### APPROVED — Show a linked-case follow-up action after protocol finalization

**User-impact note:** Target users are Swiss construction owners, contractors, and project admins finalizing a protocol that is linked to an existing defect case. The workflow moment is immediately after the protocol is saved/export-ready, when the user needs to continue legal follow-up in the case ledger. This is not just incremental polish: linked-case checklist sync already makes protocol finalization legally meaningful, and the UI should guide users to verify the updated case readiness/next action rather than leaving the audit loop implicit.

**Engineering task:** In `app/dashboard/page.tsx`, render a localized linked-case follow-up panel on step 3 when `effectiveSelectedCase` is available, using `buildVaultProjectCasesHref` to link to `/dashboard/cases?q=<project>`. Add localized keys in `locales/index.ts` and focused coverage in `__tests__/dashboard-linked-case-retry.test.tsx` proving that a finalized linked protocol exposes the follow-up link while standalone finalization does not. Run `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Add a dashboard next legal action summary for the selected linked case

Valuable Medium follow-up toward a legal command center, but broader than today's success-state bridge because it needs a concise contract for sidebar copy, selected-case checklist progress, and deadline context across all wizard steps.

### BACKLOG — Build a unified audit-ready activity timeline across cases, protocols, vault evidence, and reminders

Strategically strongest, but too broad for one daily PR. The smallest credible future phase-1 remains a read-only derived activity helper for one case/card after protocol → case → vault handoffs are consistently visible.

## Rejected

None.

## Result

Implemented on branch `pipeline/2026-07-14-linked-protocol-follow-up`:

- Dashboard protocol success state now shows a localized linked-case follow-up panel when the finalized protocol remains tied to a loaded case.
- The follow-up action reuses the existing cases search handoff (`/dashboard/cases?q=<project>`) so users can review the case checklist and next legal steps after protocol finalization.
- Standalone protocol finalization remains unchanged and does not show a misleading linked-case CTA.

## Product review after implementation

The shipped slice matches the approved user-impact note: it is visible immediately after a legally relevant protocol is saved, connects the user back to the case ledger where linked-case readiness is tracked, and avoids changing persistence, PDF generation, deadline math, or checklist synchronization semantics.

## Validation

Passed:

- `npm run test -- __tests__/dashboard-linked-case-retry.test.tsx` — passed (22 tests).
- Initial `npm run test` exposed a likely pre-existing grouped-run flake in `__tests__/deadlines-share-link.test.tsx`; the focused file rerun passed (21 tests), then full suite rerun passed.
- `npm run test` — passed (31 files, 242 tests) on rerun.
- `npm run lint` — passed.
- `npm run build` — passed.

## PR link

https://github.com/mjordi/swiss-construction-compliance/pull/161
