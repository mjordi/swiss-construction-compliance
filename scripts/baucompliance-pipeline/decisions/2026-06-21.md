# Daily Product Improvement Decision — 2026-06-21

## 7-day calibration

Recent first-parent merged PRs on `origin/main`:

- #137 `pipeline/2026-06-20-login-duplicate-submit-guard` — focused auth duplicate-submit guard with tests.
- #136 `pipeline/2026-06-19-invalid-case-url-cleanup` — small URL cleanup/retry reliability slice.
- #135 `pipeline/2026-06-18-supabase-client-stability` — focused Supabase client identity stabilization.
- #134 `pipeline/2026-06-17-vault-nav` — focused navigation handoff.
- #133 `pipeline/2026-06-16-cases-supabase-memo` — focused client memoization.
- #132 `pipeline/2026-06-15-vault-pending-navigation` — focused pending-navigation lock.
- #131 `pipeline/2026-06-14-login-label-accessibility` — small auth-form accessibility improvement.

This calibration points to one narrow, reviewable S-sized improvement today. No open PRs were returned by `gh pr list`, so there is no active branch overlap, but yesterday's login work makes a different surface preferable.

## PM scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Label the protocol defect input for accessible review | 7 | 10 | 8 | 2 | 10 | APPROVED |
| M — Lock the dashboard "new protocol" reset during PDF generation | 7 | 7 | 8 | 4 | 7 | BACKLOG |
| L — Create a reusable form-field accessibility contract for dashboard workflows | 8 | 4 | 9 | 7 | 5 | BACKLOG |

## Final decisions

### APPROVED — Label the protocol defect input for accessible review

Rationale: The protocol wizard is a core product workflow, the gap is directly user-visible to assistive technology, and the change is tightly scoped with focused regression coverage. This is the best match for the recent daily throughput.

**Engineering task:** In `app/dashboard/page.tsx`, add a stable `id` to the defect textarea and associate the visible `defect-detected` label with `htmlFor`. Add a focused regression test in an existing dashboard test file proving Step 2 exposes the textarea via `getByLabelText("defect-detected")` and that typing through that query updates the textarea.

### BACKLOG — Lock the dashboard "new protocol" reset during PDF generation

Rationale: Valuable async race hardening, but it requires controlled PDF-promise coverage and review of success-step reset semantics. It is larger than today's accessibility slice.

### BACKLOG — Create a reusable form-field accessibility contract for dashboard workflows

Rationale: Strategically useful, but a cross-surface migration is too broad for the calibrated daily PR size. It should be broken into several daily slices after the protocol Step 2 gap is closed.

## PR

PR link: Pending engineering
