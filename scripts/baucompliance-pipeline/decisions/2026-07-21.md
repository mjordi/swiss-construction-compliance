# Daily Product Improvement Decision — 2026-07-21

## 7-day merged-PR calibration

Recent first-parent merged PRs in the last 7 days:

- #167 `pipeline/2026-07-20-case-notice-readiness-snapshot` — S scan-level notice draft readiness in case snapshots.
- #166 `pipeline/2026-07-19-case-evidence-readiness-summary` — S scan-level evidence readiness in case snapshots.
- #165 `pipeline/2026-07-18-linked-case-checklist-normalization` — S linked-case checklist normalization after protocol finalization.
- #164 `pipeline/2026-07-17-case-protocol-snapshot` — S scan-level linked protocol count in case snapshots.
- #162 `pipeline/2026-07-15-vault-archive-feedback` — S Vault archive/restore confirmation.
- #163 `pipeline/2026-07-16-case-reminder-badge` — S scan-level case reminder readiness badge.
- #161 `pipeline/2026-07-14-linked-protocol-follow-up` — S dashboard linked protocol follow-up handoff.

Calibration: recent throughput supports one focused frontend product slice with localized copy, a focused regression test, and full `npm run test`, `npm run lint`, and `npm run build` validation. The week remains S-sized, but the previous case-card work intentionally completed prerequisite readiness dimensions. Today can safely step up to a bounded Medium phase-1 summary because it reuses those established signals rather than adding new persistence or workflow behavior.

Open PR overlap: `gh pr list --state open` returned no open PRs, so there is no known active branch overlap.

## 14-day strategic portfolio check

- **User-facing workflow improvements vs internal fixes:** Recent work is strongly user-facing and legally relevant: reminder feedback, protocol handoffs, Vault audit context, archive/restore confirmation, linked-case checklist normalization, and scan-level Cases readiness signals.
- **S vs M vs L/phase-1 strategic choices:** Mostly S-sized slices. The repeated Medium/L theme is a unified audit/activity cockpit; the newly completed scan-level fields make a small Medium phase-1 summary timely.
- **Repeated surfaces:** Cases has been touched often, but the last few runs filled distinct legal readiness dimensions. Another Cases change is justified only if it consolidates those dimensions into a higher-level user outcome.
- **Repeated strategic themes:** The unified audit/activity timeline remains backlogged. A read-only audit-readiness summary is the next prerequisite because it establishes the concise readiness contract before chronological modeling.

## Scoring and decisions

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Add an explicit defect-documentation readiness cell to the case snapshot | 6 | 10 | 7 | 1 | 10 | BACKLOG |
| M — Add a first case audit-readiness summary row | 9 | 8 | 10 | 3 | 9 | APPROVED |
| L — Build a unified audit-ready activity timeline | 10 | 3 | 10 | 8 | 5 | BACKLOG |

## Approved

### APPROVED — Add a first case audit-readiness summary row

**User-impact note:** Target users are Swiss construction owners, contractors, and project admins scanning the Cases ledger. The workflow moment is deciding which defect case is ready for legal follow-up and which blocking artifact to complete next. This is not just incremental polish: it converts several existing legally meaningful readiness signals into a concise audit-package answer that a real user would notice before opening details.

**Engineering task:** In `app/dashboard/cases/page.tsx`, add a localized read-only audit-readiness summary at the top of each `cases-action-snapshot-*` grid. The summary must use the already normalized checklist state and existing protocol count; count readiness across defect documentation, evidence, notice draft, calendar reminder/export applicability, and linked protocol evidence; name missing items with localized labels; and avoid changing persistence, deadline math, checklist schema, filters, routes, protocol creation, Vault behavior, or reminder export behavior. Add localized keys in `locales/index.ts` and focused coverage in `__tests__/cases-checklist-persistence.test.tsx`. Run `npm run test -- __tests__/cases-checklist-persistence.test.tsx`, `npm run test`, `npm run lint`, and `npm run build`.

## Backlogged

### BACKLOG — Add an explicit defect-documentation readiness cell to the case snapshot

Useful and very safe, but weaker than today’s summary because it adds one more isolated cell rather than helping users synthesize case package readiness.

### BACKLOG — Build a unified audit-ready activity timeline

Strategically strongest, but too broad for one daily PR. The smallest credible phase-1 after today is a derived read-only timeline helper for one case surface using the new summary contract.

## Rejected

None.

## Result

Implemented on branch `pipeline/2026-07-21-case-audit-readiness-summary`:

- Case action snapshots now show a localized audit-readiness summary row before the detailed readiness cells.
- The summary counts five existing legal package prerequisites: defect documentation, evidence, notice draft, calendar reminder/export applicability, and linked protocol evidence.
- Missing package items are named from existing localized labels so users can immediately see the next blocker without opening the detail checklist.

## Product review after implementation

The shipped slice matches the approved user-impact note: a user scanning the Cases ledger now gets a concise audit-package readiness answer before reading the individual deadline, protocol, evidence, notice, and reminder cells. The implementation is read-only and does not alter checklist persistence, deadline math, reminder export behavior, filters, routes, protocol creation, or Vault behavior.

## Validation

Passed:

- `npm run test -- __tests__/cases-checklist-persistence.test.tsx` — passed (13 tests).
- `npm run test` — passed (31 files, 251 tests).
- `npm run lint` — passed.
- `npm run build` — passed.

## PR link

Pending PR creation.
