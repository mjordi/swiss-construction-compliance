# Daily Product Improvement Decision — 2026-06-19

## 7-day calibration

`git log --first-parent --merges --since='7 days ago' origin/main` shows seven recent daily pipeline merges: #129 dashboard download feedback, #130 login label accessibility, #131 case edit/cache refresh, #132 Vault pending navigation, #133 cases Supabase memoization, #134 Vault dashboard navigation, and #135 dashboard/settings Supabase client identity. The recent cadence is healthy but consistently small, test-focused frontend reliability work. `gh pr list --state open` returned no open PRs, so there is no active PR overlap. Today's approval should stay within that S-sized band and avoid another broad same-surface refactor immediately after the Supabase identity work.

## PM scoring

| Proposal | business_value | feasibility | strategic_fit | risk | phaseability | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| S — Clean invalid dashboard case handoff URLs after lookup | 7 | 9 | 8 | 2 | 9 | APPROVED |
| M — Add a dedicated stale-linked-case recovery banner for URL handoffs | 7 | 6 | 8 | 4 | 6 | BACKLOG |
| L — Unified dashboard handoff state contract across Cases, Vault, and Protocol Wizard | 9 | 4 | 9 | 7 | 5 | BACKLOG |

## Decisions

### APPROVED: S — Clean invalid dashboard case handoff URLs after lookup

Rationale: This is a narrow URL-state reliability improvement that complements recent cross-workflow navigation work without overlapping the Supabase client identity patches. It is small enough for one PR, has clear expected behavior, and can be validated with one focused regression test plus the required repo commands.

**Engineering task:** In `app/dashboard/page.tsx`, when `pendingRequestedCaseId` is present and cases have successfully loaded but no matching case exists, remove only `case` from the URL using `router.replace`, preserve unrelated query params, and leave standalone wizard state usable. Add a focused test in `__tests__/dashboard-linked-case-retry.test.tsx`.

### BACKLOG: M — Add a dedicated stale-linked-case recovery banner for URL handoffs

Rationale: Valuable UX follow-up, but it requires new localized copy and UI states; today's approved URL cleanup addresses the underlying stale-link hazard with lower risk.

### BACKLOG: L — Unified dashboard handoff state contract across Cases, Vault, and Protocol Wizard

Rationale: Strategically strong, but too broad for current daily throughput and should be split into a dedicated phase-1 plan before implementation.

## Rejected

None.

## PR

Pending.
