# Daily Product Improvement Proposals — 2026-06-11

## PM insights

- The last 7 days show a healthy cadence of small, focused reliability/UX PRs (#121–#127), mostly one narrow surface plus focused tests.
- Recent work improved URL/state handoffs, reminder presets, stale async feedback, and settings labels. The safest next slice is another tightly scoped accessibility/reliability fix rather than a broad workflow rewrite.
- There are no currently open PRs, so overlap risk is low. The dashboard handover wizard still has visible labels that are not programmatically associated with text inputs, unlike the settings page fixed yesterday.

## Small (S): Associate dashboard handover wizard field labels with inputs

- **Problem:** The handover wizard’s Project, Contractor, and Client fields render adjacent labels without `htmlFor`/`id`, making them harder to target for screen readers, browser autofill, and regression tests.
- **Scope:** Add stable ids to those three inputs, wire their labels with `htmlFor`, and add a focused RTL regression test using `getByLabelText`.
- **Expected impact:** Improves accessibility and testability on the core protocol creation path with minimal product risk.
- **Risk:** 2/10 — markup-only behavior with focused test coverage.
- **Effort/size:** S.

## Medium (M): Add stale-success protection to handover PDF download feedback

- **Problem:** The final protocol download action has a loading state but no user-visible failure/success feedback contract if PDF generation fails or if the user starts a new protocol while generation is pending.
- **Scope:** Add explicit translated download feedback, stale-completion guards, and tests around failed and overlapping download attempts.
- **Expected impact:** Clearer operator trust during on-site handover export.
- **Risk:** 5/10 — touches finalization/export UX and PDF mocking.
- **Effort/size:** M.

## Large / strategic (L): Case-linked protocol completion dashboard

- **Problem:** Protocol creation, case follow-up checklist progress, and vault evidence counts are spread across multiple pages; operators lack one completion view for a case.
- **Scope:** Add a case detail/workspace route that aggregates protocol count, checklist progress, vault links, and deadline status, then link cases/vault/dashboard into it.
- **Expected impact:** Strong strategic workflow consolidation for compliance teams.
- **Risk:** 8/10 — new route, multiple data sources, navigation semantics, and broader test matrix.
- **Effort/size:** L.
