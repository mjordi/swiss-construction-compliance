# Daily Product Improvement Proposals — 2026-06-14

## PM insights

- The worktree started clean on `main` at `886fe52`, aligned with `origin/main`.
- The last 7 days of merged work show a steady cadence of small, reviewable UI reliability/accessibility fixes: dashboard PDF download feedback, dashboard wizard label wiring, settings labels, password save race handling, stale deadline clearing, and reminder `aria-pressed` coverage.
- There are no currently open GitHub PRs, so overlap risk is low.
- The strongest repo signal is to keep today's work narrow, testable, and complementary to the recent accessibility/reliability stream rather than starting a larger workflow rewrite.

## Small (S) — Associate login form labels with their inputs

- **Problem:** The login/signup page visually labels the name, email, and password fields, but the labels are not programmatically associated with their inputs. Screen-reader users and automated accessibility tests cannot reliably query the fields by their visible labels.
- **Scope:** Add stable `id`/`htmlFor` wiring for the login/signup form fields and focused tests that verify the email/password labels, signup-only name label, and sign-in/sign-up mode switching.
- **Expected impact:** Improves accessibility and form usability on the key entry point without altering auth behavior.
- **Risk (1-10):** 2 — localized markup/test change.
- **Effort/size:** S.

## Medium (M) — Add pending feedback to Vault archive/restore mutations

- **Problem:** Vault archive/restore buttons disable while a project status mutation is pending, but their visible label does not indicate that work is in progress. Users may not understand why the project moved/locked temporarily.
- **Scope:** Add translated pending labels/spinner treatment for archive/restore buttons, keep per-row mutation state scoped, and extend Vault tests for pending state and retry recovery.
- **Expected impact:** Clearer feedback on project archive/restore operations and less confusion during slow network saves.
- **Risk (1-10):** 4 — touches an existing async mutation surface with several focused tests.
- **Effort/size:** M.

## Large / strategic (L) — Unified compliance action center

- **Problem:** Follow-up tasks, deadlines, vault documents, and protocol generation remain spread across separate dashboard surfaces. Users must navigate between them to decide the next best compliance action.
- **Scope:** Design and implement a first-phase dashboard action center that aggregates overdue/triage cases, missing evidence, pending protocols, and upcoming reminders, with direct handoffs into existing surfaces.
- **Expected impact:** Strong strategic product value by turning BauCompliance into a daily command center for construction compliance work.
- **Risk (1-10):** 8 — cross-surface data modeling, UX, i18n, and test coverage; too large for one normal daily run unless sliced aggressively.
- **Effort/size:** L.
