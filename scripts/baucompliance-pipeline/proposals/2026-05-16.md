# Research Proposals — 2026-05-16

## PM Insights
- The last 7 days of merged work on `origin/main` show a steady cadence of narrow, user-facing UX and reliability patches: cases checklist persistence (#98), deadlines share-link feedback (#99), cases share-link feedback hardening (#100), vault-to-cases follow-up links (#101), cases initial-load retry (#102), and dashboard linked-case retry (#103).
- Recent merged size stays inside a practical daily envelope: typically 5–8 files changed and roughly +300 to +540 lines when a patch stays on one workflow, adds focused tests, and updates locales/artifacts.
- There are no open PRs right now, so overlap risk is low; the main PM constraint is keeping today’s work on one existing dashboard surface with a clear recovery/value story.
- The Tech Vault still has a blocking aggregate-load error state with no in-product retry path, forcing users to hard-refresh if case/protocol loading fails.
- The Vault also does not preserve its current tab/query state in the URL, which makes it harder to hand off filtered evidence views or return to the same review slice after navigation.
- The canonical pipeline prompt is still missing from the checked-in tree and `origin/main`, but recoverable from historical commit `98034e9`; that remains an operational concern rather than today’s highest-value product improvement.

## Proposal S — Add a retryable aggregate-load recovery state to the Tech Vault overview
- **Title**: Add a retryable aggregate-load recovery state to the Tech Vault overview
- **Problem**: When the Vault’s aggregate cases/protocols load fails, users hit a dead-end error card with no direct recovery path except a full page refresh.
- **Scope**: Add a localized retry CTA and tighter in-flight/error-state handling to `/dashboard/vault`, while preserving the current empty-state behavior once data loads successfully.
- **Expected impact**: Reduces dead ends on the evidence workflow and aligns recovery behavior with the repo’s other recent dashboard reliability fixes.
- **Risk (1-10)**: 3
- **Effort/size**: Small

## Proposal M — Preserve Vault tab and search state in the URL for shareable evidence views
- **Title**: Preserve Vault tab and search state in the URL for shareable evidence views
- **Problem**: Users lose their current Vault tab and search context on navigation/refresh, and they cannot share a filtered evidence view with teammates.
- **Scope**: Sync the Vault’s active tab and query into URL params, hydrate them on load, and add focused regression coverage for back/forward and shareable filtered views.
- **Expected impact**: Improves navigation continuity and lightweight collaboration on evidence review without changing core data flows.
- **Risk (1-10)**: 4
- **Effort/size**: Medium

## Proposal L — Build a unified case workspace across timeline, audit, and vault workflows
- **Title**: Build a unified case workspace across timeline, audit, and vault workflows
- **Problem**: Users still have to coordinate compliance follow-up, protocol generation, and evidence review across separate pages when managing one dispute or handover issue.
- **Scope**: Introduce a shared case workspace that consolidates follow-up status, linked protocols, evidence, and next actions into one cross-workflow view.
- **Expected impact**: Meaningfully improves product coherence for end-to-end claims handling.
- **Risk (1-10)**: 7
- **Effort/size**: Large
