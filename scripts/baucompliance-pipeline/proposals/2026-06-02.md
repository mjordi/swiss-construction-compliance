# Daily Product Improvement Proposals — 2026-06-02

## PM insights

- The last week of merged pipeline work is dominated by focused reliability and workflow-handoff patches: vault handoffs/archive actions, dashboard draft persistence, calculator stale results, settings save races, and case create pending guards.
- `origin/main` already contains yesterday's case-create pending guard, so today's safest adjacent slice should avoid the same create surface and look for another narrow async workflow lock.
- There are no currently open PRs, reducing overlap risk, but recent throughput still supports one compact, testable change rather than a broad dashboard refactor.

## S — Lock protocol finalization while save is pending

- **Problem:** The dashboard protocol finalization path has a synchronous in-flight guard, but the step-2 form can still expose secondary controls during the pending save. In particular, the no-defects checkbox remains editable while the protocol insert/checklist sync is underway, which can make the visible form drift from the submitted snapshot.
- **Scope:** Disable the no-defects checkbox while finalization is generating and add regression coverage that double-clicking finalize submits only once while the step-2 controls are locked.
- **Expected impact:** Improves reliability of the core handover-protocol workflow and matches the recent async mutation hardening pattern.
- **Risk (1-10):** 2
- **Effort/size:** S

## M — Extract shared pending-mutation lock helpers for dashboard workflows

- **Problem:** Cases, vault, and dashboard protocol flows each maintain local pending state and refs, which makes subtle lock omissions easy to reintroduce.
- **Scope:** Introduce a small hook/helper for duplicate-submit suppression and control disabling across one or two dashboard surfaces, then migrate representative flows.
- **Expected impact:** Reduces recurring async-state bugs and simplifies future reliability patches.
- **Risk (1-10):** 5
- **Effort/size:** M

## L — Build a guided compliance action center

- **Problem:** The product now has cases, vault projects, protocols, checklist progress, and deadlines, but users still need to mentally stitch together the next best compliance action across surfaces.
- **Scope:** Add a strategic dashboard action center that aggregates urgent cases, missing evidence, protocol status, and vault document gaps with cross-workflow CTAs.
- **Expected impact:** High product value: turns BauCompliance from separate tools into a guided compliance cockpit.
- **Risk (1-10):** 8
- **Effort/size:** L
