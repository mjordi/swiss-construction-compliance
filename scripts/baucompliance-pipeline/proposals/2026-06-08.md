# Daily Product Improvement Proposals — 2026-06-08

## PM insights

- Recent first-parent history shows a healthy cadence of small, focused pipeline PRs over the last 7 days (#118–#123), mostly UI reliability and URL/share/reminder-state fixes.
- The dashboard deadline calculator recently gained share-link/reminder behavior, but its calculated result can remain visible after the source acceptance date is edited. That can make exported ICS/share actions drift from what the form appears to be preparing.
- No open PRs are currently listed, so overlap risk is low.

## S — Clear stale dashboard deadline results after source date edits

- **Problem:** After calculating dashboard deadlines, editing the acceptance date leaves the old deadline results and export/share controls visible until the user recalculates. The page already tracks the last calculated date for share links/ICS, so the visible results can silently drift from the edited input.
- **Scope:** In `app/dashboard/deadlines/page.tsx`, clear derived `deadlines` and `calculatedAcceptanceDate` when the acceptance date input changes; keep reminder presets intact. Add a regression test in `__tests__/deadlines-share-link.test.tsx` asserting stale results/export controls disappear after an edit.
- **Expected impact:** Prevents users from copying/exporting stale deadline data and makes the calculator state easier to trust.
- **Risk:** 2/10
- **Effort/size:** S

## M — Add dashboard deadline per-card handoff links to cases

- **Problem:** The dashboard deadline page shows urgent/expired deadline cards, but users must manually navigate to cases to act on them.
- **Scope:** Add a focused CTA from urgent/expired deadline cards to `/dashboard/cases` using existing query/search patterns, plus tests for URL semantics and accessibility.
- **Expected impact:** Stronger workflow bridge from deadline insight to case action.
- **Risk:** 5/10
- **Effort/size:** M

## L — Unified deadline workspace phase 1

- **Problem:** Public and dashboard deadline tools share concepts but remain separate surfaces with duplicated mental models.
- **Scope:** Phase 1 would define a shared deadline view-model and migrate one surface to it, preserving existing behavior and tests.
- **Expected impact:** Strategic reduction in duplicated calculator behavior and easier future legal-deadline feature work.
- **Risk:** 7/10
- **Effort/size:** L
