# Research Proposals — 2026-05-17

## PM Insights
- The last 7 days of merged work on `origin/main` show a consistent cadence of tightly scoped UX/reliability patches: deadlines share-link feedback (#99), cases share-link hardening (#100), vault-to-cases follow-up links (#101), cases load retry (#102), dashboard linked-case retry (#103), and vault load retry (#104).
- Recent merged scope stays inside a practical daily envelope: single-surface dashboard changes with focused tests, usually small enough to review in one PR without broad architecture churn.
- There are no open PRs right now, so overlap risk is low; the main PM constraint is continuing to ship narrow, complementary workflow bridges instead of larger state rewrites.
- After the recent vault reliability work, the most visible remaining product gap in that surface is that the Tech Vault still renders “new/create project” CTAs that do not navigate anywhere.
- The Vault also still drops tab/search context on refresh or sharing, which is valuable but meaningfully broader than a same-day navigation fix.
- The canonical pipeline prompt file is still missing from the checked-in tree and `origin/main`, but recoverable from historical commit `98034e9`; that remains an operational concern rather than today’s top product improvement.

## Proposal S — Link Tech Vault create-project CTAs to the dashboard workflow
- **Title**: Link Tech Vault create-project CTAs to the dashboard workflow
- **Problem**: The Vault shows prominent “new/create project” calls to action, but both are dead UI elements, so users who discover missing evidence/project state there are not taken to the place where they can actually start new work.
- **Scope**: Convert the header CTA and dashed create card in `/dashboard/vault` into working links to `/dashboard`, using one shared helper and focused regression coverage.
- **Expected impact**: Removes a visible dead end and creates a clean bridge from evidence review into the existing project/protocol creation workflow.
- **Risk (1-10)**: 2
- **Effort/size**: Small

## Proposal M — Preserve Vault tab and search state in the URL for shareable evidence views
- **Title**: Preserve Vault tab and search state in the URL for shareable evidence views
- **Problem**: Users lose their Vault tab/search context on refresh and cannot share a filtered evidence slice with teammates.
- **Scope**: Sync the active Vault tab and query into URL params, hydrate on load, and add focused tests for back/forward/share behavior.
- **Expected impact**: Improves navigation continuity and collaboration on evidence review.
- **Risk (1-10)**: 4
- **Effort/size**: Medium

## Proposal L — Build a case workspace that unifies cases, protocols, and vault evidence
- **Title**: Build a case workspace that unifies cases, protocols, and vault evidence
- **Problem**: Users still have to coordinate follow-up, protocol creation, and evidence review across multiple pages when handling one dispute or handover issue.
- **Scope**: Introduce a shared case workspace with linked protocol/evidence activity, next actions, and status rollups across workflows.
- **Expected impact**: Significantly improves end-to-end product coherence for claims handling.
- **Risk (1-10)**: 7
- **Effort/size**: Large
